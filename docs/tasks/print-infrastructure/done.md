# Done — the print line

Newest first: date, what shipped, PR. Backlog: [`backlog.md`](backlog.md).

## Before the split (2026-09-25)

The line as it stands — `bambu slice compose`, the flag reference gate, `bambu ams` +
filament-sync, colour regions → per-body export → AMS slots, `slice coaster` — is in
[`../../tasks/done.md`](../../tasks/done.md), Snapshot 10, sections "Plates and the bambu CLI
(P4.x)" and "Coaster colour regions → per-body export → AMS filament map"; the bring-up and
dispatch gates are in Snapshot 9.
