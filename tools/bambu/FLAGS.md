<!-- GENERATED by `bambu dump-flags` — do not edit by hand. Regenerate with `make bambu-flags`. -->

# `bambu` CLI — flag reference

Every subcommand, positional argument, and option the CLI exposes, generated from the commander
tree. Hook `45-bambu-flags` blocks a commit whose checked-in copy drifted from this — add a flag,
run `make bambu-flags`. Live help stays authoritative per verb: `bambu <group> <verb> --help`.

### `bambu` (global)

| Flag | Description |
|---|---|
| `-V, --version` | output the version number |

### `bambu setup`

wire the X2D to Claude Code and preflight it

### `bambu setup discover`

passively find Bambu printers broadcasting on the LAN (zero-touch; safe during a print)

| Flag | Description |
|---|---|
| `--timeout <ms>` | how long to listen for a broadcast |
| `--port <port>` | UDP port the printer broadcasts to |

### `bambu setup doctor`

preflight every dependency and connection; names what's missing

| Flag | Description |
|---|---|
| `--probe-mcp` | also attempt a live griches MCP handshake (needs config + network) |

### `bambu setup mcp`

show the .mcp.json griches entry to add (real file is gitignored — carries the token)

### `bambu setup studio`

detect installed Bambu Studio (stable/beta) and where to get it

### `bambu status`

read-only printer status (proves transport)

### `bambu status show`

one-shot printer status over first-party MQTT

| Flag | Description |
|---|---|
| `--json` | print the raw report frame instead of a summary |

### `bambu status monitor`

poll status on an interval (Ctrl+C to stop)

| Flag | Description |
|---|---|
| `-i, --interval <seconds>` | seconds between polls |
| `--json` | print the raw report frame instead of a summary |

### `bambu status camera`

capture a chamber snapshot (needs ffmpeg) — via the MCP transport

| Flag | Description |
|---|---|
| `-o, --out <path>` | output JPEG path |

### `bambu filament`

read-only view of loaded filament (AMS trays + external spool)

| Flag | Description |
|---|---|
| `--json` | print the raw ams/vt_tray frame instead of a summary |

### `bambu header`

auto-pull the bench-sheet profile header from the printer (+ a sliced --plate .3mf)

| Flag | Description |
|---|---|
| `--plate <file.3mf>` | a sliced plate — fills machine / layer height / profile / slicer version |
| `--json` | emit the header as a machine object (what `print send --record` consumes) |

### `bambu slice`

headless slicing via the BambuStudio CLI

### `bambu slice plate`

slice a .stl/.3mf/.step/.obj into a sliced .3mf

| Argument | Required | Description |
|---|---|---|
| `model` | yes |  |

| Flag | Description |
|---|---|
| `-o, --out <file>` | output filename (default: <model>.sliced.3mf) |
| `-d, --outputdir <dir>` | output directory (default: alongside the input) |
| `-s, --settings <names|paths>` | machine + process, semicolon-joined — preset display names (resolved to the bundled JSON) or JSON paths |
| `-f, --filament <names|paths>` | filament, semicolon-joined — preset display name (resolved to the bundled JSON) or JSON path |
| `-p, --plate <n>` | plate index to slice, 0 = all |
| `--filament-map-mode <mode>` | X2D dual-nozzle filament grouping: saving (Filament-Saving, default) \| quality \| manual — only affects a plate with ≥2 filaments |
| `--arrange` | arrange objects before slicing |
| `-t, --timeout <seconds>` | slice timeout in seconds |
| `--strict` | exit non-zero if the slicer prints any warning/error-looking line |
| `--dry-run` | print the exact BambuStudio invocation and exit |

### `bambu slice open`

open a sliced plate (or model) in the Bambu Studio GUI to eyeball before dispatch

| Argument | Required | Description |
|---|---|---|
| `plate` | yes |  |

### `bambu slice compose`

compose many bikar-rendered pieces onto one X2D plate (a manifest → one sliced .3mf)

| Argument | Required | Description |
|---|---|---|
| `plate.yaml` | yes |  |

| Flag | Description |
|---|---|
| `-o, --out <file>` | output filename (default: <manifest>.plate.3mf) |
| `-d, --outputdir <dir>` | output directory (default: current dir) |
| `-s, --settings <names|paths>` | machine + process, semicolon-joined — overrides the manifest profile (preset display names or JSON paths) |
| `-f, --filament <names|paths>` | filament, semicolon-joined — overrides the manifest profile (preset display name or JSON path) |
| `--bed <name>` | bed footprint for the fit pre-check (x2d = 256×256 mm) |
| `--arrange` | auto-arrange the objects on the plate (libnest2d in the slicer) |
| `--no-arrange` | do not auto-arrange (objects keep authored positions) |
| `--no-record` | skip scaffolding the draft plate record |
| `-t, --timeout <seconds>` | slice timeout in seconds |
| `--strict` | exit non-zero if the slicer prints any warning at all |
| `--dry-run` | render + pre-check + print the exact BambuStudio invocation, but do not slice |

### `bambu slice coaster`

assemble a multi-filament COLOUR plate (bikar --format parts → per-region AMS 3MF) for the X2D

| Argument | Required | Description |
|---|---|---|
| `plate.yaml` | yes |  |

| Flag | Description |
|---|---|
| `-o, --out <file>` | output filename (default: <manifest>.plate.3mf) |
| `-d, --outputdir <dir>` | output directory (default: current dir) |
| `-s, --settings <names|paths>` | machine + process, semicolon-joined — overrides the manifest profile |
| `-f, --filament <name|path>` | the plate's default (slot-1) filament — overrides the manifest profile |
| `--bed <name>` | bed footprint for the fit pre-check (x2d = 256×256 mm) |
| `--pinch <strategy>` | how a coaster pinch is split: fillet\|merge\|error (bikar --format parts) |
| `--max-slots <n>` | AMS capacity to cap the logical slot count at (default one unit, 4) |
| `--no-verify-geometry` | skip the headless tag-stripped geometry slice |
| `--no-record` | skip scaffolding the draft plate record |
| `-t, --timeout <seconds>` | geometry-verify slice timeout in seconds |
| `--dry-run` | render + map + pre-check + print the plan, but do not assemble or verify |

### `bambu print`

catalog, capture, dispatch and control prints — `list`/`capture` are local & read-only; `send`/`pause`/`stop` move real hardware and are owner-gated

### `bambu print send`

upload a sliced .3mf (FTPS) + start it (MQTT) — OWNER-GATED, confirm-before-send

| Argument | Required | Description |
|---|---|---|
| `plate` | yes |  |

| Flag | Description |
|---|---|
| `--record` | scaffold a draft print record under .bambu/records/ |
| `--slug <slug>` | slug for the record run name (default: derived from the plate) |
| `-O, --object <spec>` | printed object as bikar:<path>[=ENTRY] (repeatable) — pins R1 provenance in the record |
| `--plate <n>` | plate index inside the .3mf to print (default 1 → Metadata/plate_1.gcode) |
| `--bed-type <type>` | [X2D-UNCONFIRMED] plate profile: auto\|cool_plate\|eng_plate\|hot_plate\|textured_plate (default auto) |
| `--ams-mapping <spec>` | [X2D-UNCONFIRMED] filament→slot map: comma-ints e.g. "0" or "-1,0", or "none" (default "0") |
| `--md5 <hex>` | [X2D-UNCONFIRMED] .3mf checksum for firmware that validates it (default empty) |
| `--no-bed-leveling` | skip auto bed-leveling before this print |
| `--no-flow-cali` | skip flow calibration before this print |
| `--no-vibration-cali` | skip vibration calibration before this print |
| `-y, --yes` | skip the confirmation prompt (still logs the owner-gate notice) |
| `--allow-unverified` | dispatch a plate with no warnings-capture sidecar (high-bar override of the fail-closed gate) |
| `--dry-run` | print the exact FTPS target + MQTT payload without connecting or dispatching |

### `bambu print capture`

read the printer's MQTT device report → scaffold a DRAFT record (counts a GUI print) — read-only, no owner gate

| Flag | Description |
|---|---|
| `--slug <slug>` | slug for the record run name (default: the printer's subtask_name, else 'captured-print') |
| `--plate <file>` | the sliced .3mf that was printed — fills the slice-side profile fields (machine/nozzle/layer/profile) |
| `-O, --object <spec>` | printed object as bikar:<path>[=ENTRY] (repeatable) — pins R1 provenance in the record |
| `--json` | print the built actuals + raw frame as JSON instead of scaffolding a record |

### `bambu print list`

list every print record — what came off the plate and (--how) how it was printed

| Flag | Description |
|---|---|
| `--shipped` | only the shipped docs/prints/ records |
| `--drafts` | only the gitignored .bambu/records/ drafts |
| `--how` | show the process identity (machine/material/nozzle/layer/profile) instead of outcome |
| `--settles <cal>` | only records settling a bet whose CAL id contains this (e.g. CAL-FIT) |
| `--material <m>` | only records printed in a material containing this (e.g. PLA) |
| `--machine <m>` | only records printed on a machine containing this (e.g. X2D) |
| `--status <s>` | only records at this exact lifecycle status (e.g. measured) |
| `--json` | emit the records as JSON instead of a table |

### `bambu print pause`

pause the running print

### `bambu print resume`

resume the running print

### `bambu print stop`

stop the running print (confirms first)

| Flag | Description |
|---|---|
| `-y, --yes` | skip the confirmation prompt |

### `bambu validate`

gate a mesh / plate / record before it ships

### `bambu validate mesh`

bikar min-strut/FDM --check on a .bkr source

| Argument | Required | Description |
|---|---|---|
| `model` | yes |  |

### `bambu validate plate`

the 23-rung calibration expectation table (calibration-design §7, D-014)

### `bambu validate record`

run the prints gate over a records dir (default: .bambu/records/)

| Argument | Required | Description |
|---|---|---|
| `dir` | no |  |

### `bambu validate sliced`

gate a sliced .3mf: realized brim/support/raft, object count, header; report time/length/grams

| Argument | Required | Description |
|---|---|---|
| `3mf` | yes |  |

| Flag | Description |
|---|---|
| `--expect-objects <n>` | fail unless the plate holds exactly N objects |
| `--bare-plate` | shorthand for --no-brim --no-supports --no-raft (the Plate-1 expectation) |
| `--no-brim` | fail if any Brim feature is in the realized gcode |
| `--no-supports` | fail if any Support feature is in the realized gcode |
| `--no-raft` | fail if any Raft feature is in the realized gcode |
| `--machine <substr>` | fail unless printer_model contains this (e.g. X2D) |
| `--nozzle <d>` | fail unless every nozzle_diameter equals this (e.g. 0.4) |
| `--density <g/cm3>` | PLA density for the grams estimate (default 1.24) |
