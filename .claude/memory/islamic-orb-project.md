---
name: islamic-orb-project
description: "Cross-repo Islamic geometric orb project — bikar DSL 3D extension, qiyas validation, 3d-models publishing; M0–M5 + Orb Lab P0–P2 shipped/deployed; C1/W1/C2/W2 + Lego Lab R0 + calibrate machinery merged; mural (UC20) deployed 2026-08-02; `footprint outline` mode + Rosette-Brick.bkr (LG-B2) SHIPPED 2026-08-02; text-emit T-series fully closed 2026-08-05 (T0–T3 + D-023 slashed-zero + D-024 catalog-gate-declined + D-025 charset-blocks); LDraw colour shipped 2026-08-06 — #104/D-026 per-part colour (grounded names+codes) then #105/D-027 per-brick stud colour (pins painted apart from body via mixed-colour inline part); LDraw thumbnail CLI + `validate-render` skill shipped 2026-08-07 (#106/#108/#109, D-028 — .mpd→set-of-PNGs, Playwright full Chromium, hard counts + soft goldens, only counts port across backends); colour gate + catalog gate shipped 2026-08-08 (#110/#111/#112, D-029 — a third `--check` strength: nearest-of-palette visible-colour SET that ports where pixels don't, plus per-model `.notes.md` and a GPU-free catalog well-formedness hook; split at the GPU); maclado Family 3 M1–M4 merged 2026-08-08 (bikar #85–#88, `d20e3f5` = symmetric dodecahedral field via the divisor trick; doc v2 with §2 K1 correction = 3d-models #61 `ae264ce`); maclado M5 print+gallery SHIPPED 2026-08-08 (bikar #89 `eb4f19c` = `base wheelfield` DSL + two published presets; 3d-models #62 `83d67d3` = gallery entries + UC22 + P8 print sheet); M4b STEP 1 shipped 2026-08-08 (bikar #90 `856db18` = placeSpiralChain greedy chain search; 3d-models #63 `f8bbcda` = doc M4b bullet + DOF argument + §9.1 rewrite); M4b CLOSED 2026-08-08 as D-030 documented partial (bikar #91 `0fe0cea` = gap decomposition + clustering; 3d-models #64 `aded326` = D-030 + doc v3 — greedy chain's separations are a continuum, 34 tiles→33 classes); M4c quantized-separation spike MERGED 2026-08-11 as D-031 (user chose Option A; bikar #92 `ec4518b` = latticeWalk + polygonal-cut generalization, 3d-models #65 `5df1171` = D-031 + §8 M4c — 18-wheel walk: 13 tiles→4 classes on 2 separations vs the chain's 33/32; quantization delivers; new pending user: whether a partial lattice orb is worth SHIPPING); M4d overlap spike MERGED 2026-08-11 as D-032 (bikar #93 `f7bddb7` = maclado-overlap.ts rim-crossing instrument + 10 tests, 3d-models #66 `6ab47e3` = D-032 + §8 M4d + status v4 — tangency touches, overlap weaves; field window ρ 1.15–1.25 gives all 30 pairs the clean 2-crossing weave; new pending user: whether to BUILD the welded woven-overlap orb); M4e woven-overlap orb BUILT + SHIPPED 2026-08-15 as D-033 (bikar #94 `f3cb04c` = maclado-woven.ts welded graph + `overlap` DSL + Maclado-9-Overlap.bkr preset, public surface 118→119; 3d-models #68 `1f13a9b` = D-033 + §8 M4e + gallery entry + UC22, deployed gh-pages `728d1ac` — 60 closed ribbon loops over 420 crossings, D-032's parity risk resolves positively, the pending BUILD decision is CLOSED); Q-series qiyas validation IN FLIGHT 2026-08-15 (Q0–Q4, Q6, Q2b done — bikar #96/#97/#98/#99 merged, qiyas #11 merged, qiyas #12 ribbon_pass shape type awaiting lint-test; D-034; **Q5 CI wiring BUILT as bikar #100** = sweep script + comparing test + orb-validate.yml, and its first sweep found 3 of 5 woven orbs drawing NO ribbon views; qiyas #12/#13 merged and **v0.2.0 tagged + published to GHCR 2026-08-16**, so #100 now fails at ONE step — `docker pull` `denied`, awaiting the UI-only GHCR Actions-access grant; **Q2c MEASURED 2026-08-16** = 1,994 of 2,334 `regular_polygon` emissions across the 135-SVG corpus provably came from the authoritative-sides short-circuit (fail the radius or angle gate, all at confidence 1.0), fix direction unchosen; Q4a/Q4b unstarted); **Q-SERIES FULLY CLOSED 2026-08-16** — Q4b resolved by measuring rather than changing the constant (encoder floor max_drift 0.0001–0.0003 vs max_dist 0.028284; all 30 drops are shapes the encoder never produced, unmatched-by-distance=0), qiyas **v0.3.0 tagged + published to GHCR**, contract **canonical v1.5 ACCEPTED** (sacred-patterns#36 `8a9e43b`) and both mirrors cascaded (bikar#101 `2fc4d6e`, qiyas#17 `a77ed52`), bikar Q4b gate merged (#102 `6157792`) with orb-validate green in CI against the pinned image; **O-1 rosette encoder gap CLOSED 2026-08-17 as D-035** — the 0.954/0.975 shortfall was qiyas's reconciliator bucketing edge-adjacent faces as duplicates on bounding boxes alone, fixed by a disjoint-interior exclusion (qiyas #18/#19, **v0.4.0** tagged + published), pin and both composites moved together in bikar #103 `b5f5463` with CI reproducing the numbers, and the corpus-wide D4 pass done in 3d-models — **both rosettes now 1.000, thirteen of fourteen orbs at 1.000**; O-7 (Star-Octa 0.9921 / Weave 0.9967 surplus, drop 0) still open; printing still on hold"
metadata: 
  node_type: memory
  type: project
  originSessionId: 27e89d38-2159-4b95-9416-70151c40cbd0
  modified: 2026-08-17T00:28:01.409Z
---

**2026-08-17 (O-1 = D-035) — the rosette encoder gap closes, and it was qiyas's defect, not bikar's geometry.** The two rosette composites had read 0.954 / 0.975 since 2026-07-26 and the entries below record them as measured; **both read 1.000 from qiyas v0.4.0 on** — the geometry never moved. `_shapes_overlap` in `qiyas:src/qiyas/stages/detectors/reconcile.py` bucketed two candidates as one when three gates agreed (centroid distance, bbox IoU > 0.6, areas within 30%), and **all three read only bounding boxes and scalars**, so a mirror-image pair of petal faces across a shared edge passed every one with ZERO interior overlap. **Raising `_BBOX_IOU_THRESHOLD` is not insufficient, it is impossible** — the 34 wrongly-bucketed pairs run IoU 0.6284 up to *exactly 1.0000* (a mirror line on an image axis gives both faces the same box). Fix = specialist exclusion #8 `_is_disjoint_interior_pair` (shapely), `_MIN_INTERIOR_OVERLAP_FRAC = 0.05` **read off a measured bimodal gap**: 120 bucketed pairs over 83 images split 86 genuine duplicates ≥ 0.9241 and 34 edge-adjacent faces at exactly 0.0000, nothing between. Four tests, the by-design failure FIRST (mirrored triangles at IoU 1.0000 stay separate), its converse (genuinely overlapping ones still merge — the exclusion is not bucketing switched off), and two crossing-band cases at 4.08%/6.19% pinning the constant from both sides. **Release chain, each link verified before the next:** qiyas #18 `2f4306d` → #19 `66458e7` (v0.4.0) → tag pushed after checking `git config user.email` is the yahoo address → `publish.yml` 32003546449 green → **image confirmed live via the GitHub packages API** (`v0.4.0,latest`, the two untagged siblings are the per-arch manifests) → bikar #103 `b5f5463` (pin + both `qiyasComposite`s + `RECORDED_DROP`'s two non-zero rows in ONE commit, because the pin is what produced the numbers) → orb-validate green 2m8s, **log read** (`swept 14 orb(s)`, `86 tests`) and **artifact downloaded**: Ubuntu reproduces macOS exactly, `drop=0 drift=0.0002` on both rosettes. **v0.4.0 is a minor and deliberately NOT for v0.3.0's reason** — no SCHEMA bump, same report shape; wire compat intact, *result* compat not. Do not read a policy out of two samples. **Local sweeping is broken on this machine and CI is the authority**: Docker would not start, and the `QIYAS_DIR` fallback dies in cairocffi because macOS strips `DYLD_FALLBACK_LIBRARY_PATH` across the `sh` hop the sweep shells out through — numbers came from direct per-orb `qiyas orb-validate` runs; stated in the commit rather than smoothed over. **D4 pass done and one thing deliberately NOT done: a superseded measurement is not a withdrawn number.** 0.954 was a correct measurement of the validator as it stood, so live claims were corrected (orb-lab-design §3 table + new footnote ², orb-lab-p2-design §4.3 badge copy) while dated records keep saying what they measured (§10's P2.6 bullet, the research file verbatim under its provenance header, and the two entries below) — and **no `WITHDRAWN` row was added to `docs_gate.py`**, whose docstring says every row is a number *found restated as fact after the withdrawal*; a row would also fire on the verbatim research file. **O-7 is untouched and that is the evidence the fix was narrow:** Star-Octa still 0.9921 and Weave-Orb 0.9967, both `drop` 0 — a *surplus*, the opposite defect, which `RECORDED_DROP` structurally cannot catch. [[pr-flow-for-all-repos]]

**2026-08-16 (Q4b + the contract cascade) — the Q-series closes, and the last question closed by measuring instead of by changing the constant.** Q4b was written as *"`CENTER_MATCH_MAX_DIAG_FRAC = 0.02` is 5–8× the ribbon field's own p05 spacing; one constant cannot serve both"* — i.e. widen/scale the acceptance radius. It was blocked on a qiyas image carrying `drop`/`max_drift`, until `resolveQiyas` turned out to take `QIYAS_DIR` as well as `QIYAS_IMAGE`, so the floor could be measured against the local checkout without waiting for a tag. **The measurement inverted the premise:** 54/54 views report `max_drift`, min/median/max 0.0001/0.0002/**0.0003**, zero views at exactly 0.0000 (so it is a real encoder measurement, not the by-construction zero of `recon_scores`) — about **100× below** `max_dist` 0.028284. Replicating the Hungarian assignment on the four affected views gave `unmatched-by-distance = 0` and `drop == len(gt) − len(encoded)` exactly: every drop is a shape the encoder **never produced**, so widening the radius recovers nothing and narrowing costs nothing. **No constant changed.** What shipped instead carries `drop`/`maxDrift` out of the report and into the gate (`RECORDED_DROP` per preset, `MAX_DRIFT_CEILING` 0.005 ≈ 17× the worst observed), proved by three by-design failures — the load-bearing one being *RosetteOrb drop 26 → 28 fails while `composite` stays 0.9541, unmoved*, which is the argument for the whole change demonstrated rather than asserted. Two traps worth remembering: **`views[].scores` is the encoder floor and `views[].recon_scores` is 0.0000 by construction** — one letter apart in a log; and **`geometric` is non-monotonic** because it averages over *accepted* pairs only, so evicting a drifted element *raises* it (all four affected views read `geometric` 0.9999 while `structural` fell to 0.81–0.91) — never gate on `geometric` without `drop` beside it. Release chain, in order, each link verified before the next: qiyas **#16 → `dfd0f76`** (v0.3.0), annotated tag **`v0.3.0`** pushed, `publish.yml` 31981235447 success, **`ghcr.io/naqshcoffee/qiyas:v0.3.0`** live; then bikar **#102 → `6157792`** (held deliberately until the tag existed, since `orb-validate.yml` lists itself in its own trigger paths and does `docker pull "$QIYAS_IMAGE"`). **orb-validate green on Ubuntu in 2m32s — and a 2m32s pass has the same shape as a skip**, so the log was read: `Tests 86 passed` (58 before) proves both new `it.each` families ran, and every `drop`/`maxDrift` matched the macOS numbers — a cross-platform reproduction of the encoder floor. Contract **v1.5 ACCEPTED** canonically (sacred-patterns#36 `8a9e43b`) and cascaded to both mirrors (bikar#101 `2fc4d6e`, qiyas#17 `a77ed52`); the cascade was **not** a copy-paste, because v1.4's "unbuilt for every other row" sentence is now wrong in both directions — per row: gt `ribbon_pass`, `data-ribbon-depth`, `data-ribbon-trimmed` **do** round-trip in CI, while **`data-strand` deliberately does not** (`_drift_ribbon_pass` omits it — strand ids are parity-solve labels, so their numeric gap measures nothing; a producer that split the same bands into wrong strands still scores 1.000) and `orb_view.representation` is read by nothing. **Still open, flagged not fixed:** qiyas `ci.yml`'s `paths-ignore: ['**.md', ...]` means `tests/test_dsl_metadata_contract.py` **cannot run on a PR that only edits the doc it parses** (ran by hand for #17, 12 passed) — a gate inverted by its own trigger; and the cell-view encoder still finds fewer contours than the gt declares on the two rosettes (4/21, 12/76, 6/66, 8/84; 12 of 14 orbs clean), pinned by `RECORDED_DROP` so it cannot widen silently.

**2026-08-15 (Q-series = D-034) — the three wheelfield orbs become openable in the Lab, and the Lab's coverage claims become sweeps instead of lists.** User directive: *"i want to be able to do option a and b and audit and fix defects that make b difficult. All three presets. Fix qiyas. build CI wiring"*. **Q0–Q4 + Q6 done; Q5 (CI wiring) and Q2b (qiyas ribbon consumer) are the open half.** Merged: **bikar #96** (Q2 — ribbon views so a wheelfield can be rendered for scoring); **qiyas #11** (Q3 — the audit fixes); **bikar #98** (Q6 — registry entries `maclado-9`/`maclado-9-weave`/`maclado-9-overlap` in `packages/lab/src/scripts.ts` with `qiyasComposite: null`, a THIRD trust-badge state "not yet qiyas-validated", and a content-matching sweep `orb-presets.test.ts` pinning the unscored trio by id); **bikar #99** (the e2e flake that blocked them). **bikar #97** (Q6 pothole 1 — `DEPTH_KNOBS = ['strut_depth','ribbon_depth']` LISTED not suffix-matched, exported `weaveDepthValue` shared with `lab/src/main.ts`, corner-testing sweep `orb-weave-guard.test.ts`) rebased onto main, awaiting checks. **D-034's tenet: a claim about EVERY preset must READ every preset — a hand-maintained list of knob names or preset ids is a K2 waiting to fire; sweep the registry.** `null` composite is deliberate: a preset nobody can open is not safer for being unscored, and an invented number would be indistinguishable from a measured one; **Q4's verdict is that no composite acceptance threshold is recordable on wheelfield views yet**, so composites stay null until Q5's CI job fills them (recording one requires deliberately editing the trio pin — a tripwire, not a drift). **The #99 e2e fix is recorded as a removed mechanism and a named failure, NOT a demonstrated fix** (never reproduced locally; ~40% ubuntu-only, 7.7–17.1% pixel diff = the scale of an arbitrary phase, not jitter → the frame moved wholesale). Two mechanisms removed: `elapsed` was read from `document.getAnimations()[0]` — whichever animation the DOCUMENT lists first, and the page has others (`.spinner`, blueprint-glow, pulse-connect) — while the delay applies to `path.cell`; and `?? 0` turned "no animation" into "at zero". Now: read a `path.cell` directly, `Number.NaN` on absent, then VERIFY — after writing the delay, read achieved phase from 3 cells spread across the lattice, require all within `SEEK_TOLERANCE_MS = 0.1` (loop-aware mod 12 s), retry 3×, else throw with the observed drift; `capture()` also reads the witness transform before AND after the screenshot ("the ring moved while the screenshot was being taken"). Shown failing BOTH directions with injected ±500 ms before being trusted. Mechanics: **Playwright must run from `packages/e2e`** (config lives there; from the repo root → "Cannot navigate to invalid URL"); **rebuild `npm run build -w @naqshcoffee/bikar-knobs` after editing constraints.ts** before lab tests (they import dist/); **`validate.py --refresh` re-pins as_of for ALL repos** and broke bikar anchors — for local drift, revert use-cases.md and hand-renumber only the drifted pointers (the anchored pointers caught my index.html +3-line drift exactly as designed); Monitor's parameter is `timeout_ms`. [[pr-flow-for-all-repos]]

**2026-08-15 (Q5) — the recorded qiyas composite gets a re-run, and the re-run immediately found a two-week silent defect.** bikar **#100** (`feat/orb-validate-ci`, `62f2c7d`) ships three pieces: `scripts/sweep-orb-validate.ts` (renders all 14 orbs, scores each view set, `QIYAS_IMAGE` xor `QIYAS_DIR` — both set is an error because they are different claims, and the resolved command is written into the output JSON), `packages/lab/tests/orb-composites.test.ts` (holds the numbers in `src/scripts.ts` to what the sweep measured), `.github/workflows/orb-validate.yml` (runs both). **The first sweep, before the workflow existed, found that `Rosette-Weave-Orb`, `Weave-Orb` and `Weave-Dodeca-Orb` drew no ribbon views at all** — `evaluateWovenOrb` never forwarded `woven.topology` as `orbWeave` (only the wheelfield branch did), so the CLI took its `if (result.orbWeave === undefined) return;` early exit. One field, silent for two weeks; **rendering 14 orbs end-to-end in one command is what surfaced it, and no single-file test could** — that is the argument for sweeping the directory rather than a list. Two design changes came out of measuring: `qiyasComposite` is now `{cells, ribbons}` with the badge showing the **min** (a mean lets healthy cells hide collapsed ribbons; inner `null` = the geometry drew no such representation, e.g. maclado-9-overlap has no cells), and the sweep carries a **shape-type histogram** per view set because the composite is type-blind (8 of 14 orbs score exactly 1.000; qiyas's gt scorer matches by centre and never reads `type`, which is how 340/370 ribbon bands read `regular_polygon` at confidence 1.0 with composite 1.000). Mutation-checked four ways. **RELEASE CHAIN DONE 2026-08-16** (user: "merge both and tag v0.2.0"): qiyas #12 merged `6c60187`, #13 merged `01eb05e`, annotated tag `v0.2.0` pushed after verifying pyproject + `__version__` + `uv.lock` all read 0.2.0 on main; `publish.yml` run 31959043087 green; `ghcr.io/naqshcoffee/qiyas:v0.2.0` published (version 1138552853, also tagged `latest`). **#100's re-run then failed at exactly one step** — GHCR login succeeded, `Pull qiyas` returned `Error response from daemon: denied`; every other check on #100 is green. So of the two blockers below, (1) is closed and (2) is now the ONLY thing standing between #100 and green. **Two user-gated blockers before #100 can go green:** (1) `ghcr.io/naqshcoffee/qiyas:v0.2.0` must be cut — the repo had **four disagreeing version claims** (`__version__` 0.0.1, pyproject 0.1.0, newest git tag v0.1.0, GHCR `:latest` = an untagged laptop-pushed v0.1.2), fixed with a bump + an agreement test in qiyas `chore/version-0.2.0`; (2) the qiyas GHCR package is **private**, so NaqshCoffee/qiyas → Packages → qiyas → Manage Actions access must grant NaqshCoffee/bikar read — there is no REST API for that, it is UI-only, and without it the `docker pull` step fails `denied` (as #100's first run did, by design and loudly). Trap worth keeping: same-length same-second source edits leave a **stale `__pycache__`** that makes a restored value look broken — `find -name __pycache__ -delete` before believing a Python red.

**2026-08-15 (#119 M4e = D-033, THE WELDED WOVEN-OVERLAP ORB BUILT) — merged both repos and deployed (bikar PR #94, squash `f3cb04c`; 3d-models PR #68, squash `1f13a9b`; gh-pages `728d1ac`); the D-032 pending user decision is CLOSED.** Built graph-first, not boolean: `bikar:packages/core/src/kernel3d/maclado-woven.ts` `buildWovenOverlapGraph` grows the M4 field to ρ·joinSeparation, splits every rim at its measured crossings (D-032's `rimCrossings` reused as the kernel's own gate — ρ=1.0 odd-parity/vertex-contact, ρ≈1.05 fuse 0.76mm, ρ≈1.30 fuse 0.39mm are BY-DESIGN refusals via extracted `assertWeldable`), welds crossings into degree-4 nodes, runs the §5.4 parity solver, sweeps ribbons. **Census, pinned by 9 tests and stable across ρ 1.15/1.20/1.25: 600 nodes, 1020 edges, degrees {2:180, 4:420}, 60 rim-crossing welds, 60 closed strands, 420 crossings (360 interior + 60 welds), watertight euler-0, 4080 verts/8160 tris, ~10.1 cm³ @ ρ=1.2 R=60 — the D-032 §5.4 parity risk RESOLVES POSITIVELY.** Two facts make the construction complete: `placeWheelInFrame` at grown unitMm ≡ `reliftWheel` (<1e-9mm, both gnomonic), and interior lattice edges never reach a neighbour's rim (2θₜ−θ′≈16.7° > valley ≈11.9°) so ONLY rim edges split. DSL: `overlap <ratio>` contextual statement on `base wheelfield` (requires `weave`, ratio>1, window judged by kernel not parser); preset `Orbs/Maclado-9-Overlap.bkr`, public-surface 118→119 with why-paragraph. D-033's reversal condition: a measured in-window inter-strand ribbon intersection between crossings forces the geometric-boolean branch. Mechanics: bikar worked in `bikar-lego-lab` WORKTREE (peer session held primary checkout; shared object store means worktree pushes resolve for 3d-models pointer gates); 25 of 26 full-suite fails were pre-existing worktree-environmental (clean-room CI proved it), the 1 real = public-surface count; e2e `animation-loop.spec.ts` red on the PR was the KNOWN screenshot flake (fails on merged main same day, passes locally 79/79) — merged despite it; use-case pin move to f3cb04c surfaced 20 anchor drifts, re-pointed by sed from the validator's own reports (NOT `--refresh`, which would pin to the peer's HEAD); doc-pointer gate wanted `bikar:patterns/...` form for the preset in the status line; site-graph gate re-pin 538→541 after the gallery insertion; deploy restamped bikar-ref clean at f3cb04c by detaching the worktree (can't checkout main — peer holds it) + `make bikar-stamp BIKAR_DIR=…` (`make orbs` alone does NOT restamp). [[pr-flow-for-all-repos]]

**2026-08-11 (#118 M4d = D-032, THE OVERLAP BRANCH MEASURED) — spike MERGED both repos (bikar PR #93, squash `f7bddb7`; 3d-models PR #66, squash `6ab47e3`); "tangency touches, overlap weaves, both need phase".** Trigger: user posted the maker's NEW 2026-08-11 photo (dense overlapping 9-star ribbon sphere) — "our orb looks nothing like this"; root diagnosis is a K1-shaped narrowing of OUR OWN SPEC: design doc §5.2 says overlapping rim arcs are WELDED, but M2 implemented tangency (separation exactly 2θ, one tip-to-tip contact) and §8 titled it "overlapped and welded ✅ Done" — every milestone through M4c stayed in the tangent branch. User chose "Overlap spike first" via AskUserQuestion. New `bikar:packages/core/src/kernel3d/maclado-overlap.ts`: `rimCrossings(a,b,radius)` (great-arc segment intersections: candidates ±normalize(n1×n2), on-arc sum test, transversality = plane angle, dedup 1e-6 rad), `checkOverlapWeld` (PASS = even count ≥2 transversal crossings clear of rim vertices with nodes ≥ strut width apart; FAILs name the regime: none/odd/vertex/fuse), `reliftWheel` (exact cap growth α′=atan(tanθ′·tanα/tanθ) — sites+spins preserved, so M4's field is AUTOMATICALLY tip-aligned), `buildOverlapField(field,ρ,strut)`. **THE VERDICT (R=60, θ≈20.905°, 2mm struts): three pair regimes** — (1) tangent (M2's pose): `checkWheelJoin` accepts but exactly ONE degenerate vertex contact, odd parity (impossible for closed curves in general position) = a touch, not a weave (the by-design FAIL); (2) tip-aligned overlap: exactly 2 transversal ~38° crossings on the facing spikes' flank edges (A0×B17, A17×B0), spin window ±8° at 85% separation — overlap INHERITS the phase discipline; (3) gear mesh: offset spins interleave spikes into valley notches with ZERO contact down to ~80% of join separation. **Field window ρ 1.15–1.25**: all 30 dodecahedral pairs in the clean one-X-per-edge regime → 60 degree-4 weld nodes (exactly what §5.4's parity solver accepts), inter-pair clearance >35mm (no degree-6 triple points), second-neighbour rims never touch through ρ=1.3; intra-pair node separation crosses the 2mm strut width between ρ 1.10 and 1.15 (0.76mm@1.05 … 3.93@1.25); ρ=1.30 = regime change (10 crossings/pair, clearances collapse) while cap-area sum passes 100% of sphere (the dense reference look). **NOT built: the welded mesh** — crossings are mid-surface rim nodes; a printable ribbon needs Family-1 radial over/under sweep or a boolean, and §5.5's manifold gate is combinatorial (blind to unwelded interpenetration). 10 tests in `maclado-overlap.test.ts`. 3d-models #66: D-032, M2 bullet retitled "joined tangent … narrower than its own title", new §8 M4d bullet, status v4, map re-pin (bikar→f7bddb7, 156 pointers valid). Artifact 5599fe22 (lattice-orb A/B/C decision) republished with a "Superseded by D-032" banner — the live user decision is now whether to BUILD the welded woven-overlap orb. Probe mechanics: a "minInterPair" that iterates ALL crossing pairs conflates intra-pair (strut constraint) with inter-pair (triple-point risk) — split them; vitest include pattern is repo-root-relative, run from bikar root. [[pr-flow-for-all-repos]]

**2026-08-11 (#117 M4c = D-030 FOLLOW-ON CLOSED) — the quantized-separation spike MERGED both repos as D-031; quantization delivers (bikar PR #92, squash `ec4518b`; 3d-models PR #65, `a83fb64`+follow-up `0ec4a83`, master `5df1171`).** User chose Option A (bounded spike) 2026-08-08 from the decision-memo artifact. New `bikar:packages/core/src/kernel3d/maclado-lattice.ts`: `latticeWalk(field,length,start)` = lowest-index-first DFS simple path along `field.adjacency` (dodecahedral graph is Hamiltonian → lengths 1‥20 all succeed, deterministic), `siteSeparations(field,sites,tol)` = sorted distinct pairwise centre arc-distances. **THE VERDICT, same instrument/R=60/default tol as D-030:** dodecahedral site set has exactly **5** distinct separations (43.78=join, 73.86, 114.64, 144.71, 188.50 mm); 18-wheel walk → 13 tiles → **4 classes [8,2,2,1] on 2 separations**; 13-wheel walk → 12 tiles → 6 classes; full 20-site field → 12 pentagonal tiles → 1 class; greedy chain (D-030) stays 34→33 on 32 separations. Walks are ASYMMETRIC partial fields, so the collapse is quantization, not symmetry — the trap stands confirmed: finite turn-angle menus do NOT quantize (rotations don't commute); finite SITE sets do, trivially. Every consecutive join exact via the divisor trick (17/17 `checkWheelJoin`). **Instrument had to be generalized:** every supporting plane of a dodecahedral vertex subset is a FACE plane of the solid (up to 5 coplanar sites) — maximal degeneracy for D-030's triangle-only hull cut (DFS-exhausted: no path subset >6 sites has a simplicial hull). `maclado-gap.ts` rewritten to polygonal hull faces: dedup coplanar supporting triples by sorted MEMBER SET (NOT rounded plane key — float jitter splits faces and breaks Euler), `GapTile.wheels` now `readonly number[]`, `hullTriangleCount`→`hullFaceCount`+`hullEdgeCount`; M4b's 34/33 verdict UNCHANGED (general-position centres still give all triangles, regression held). Validity gate = partition residual (clustered walk-9 fails at 1.8e4 mm², pinned as by-design FAIL test); cross-instrument check: full-field cut tiles are congruent to `field.fillers` built by an independent route. **D-031 does NOT ship an orb** — spike instrument only; shipped maclado remains symmetric M4/M5; whether a partial lattice orb is worth shipping (mold economy vs full field's 1-class vocabulary) is the new, now-informed user scope decision. Mechanics: eslint complexity 12→extracted `classifySides`; a nonexistent pathspec in `git add a b c` aborts the ENTIRE add silently (PR #65 initially missing its docs — caught via "1 file changed"); amend+force-push on a pushed branch is permission-DENIED → plain follow-up commit; 20-use-cases hook then wants the refreshed map staged WITH the docs. Decision-memo artifact (e6f5a122) left un-updated — source HTML lost to scratchpad cleanup; repo records are the durable ones. [[pr-flow-for-all-repos]]

**2026-08-08 (#116 M4b step 2 = CLOSE-OUT) — M4b closed as D-030 documented partial (bikar PR #91, squash `0fe0cea`; 3d-models PR #64, squash `aded326`).** New `bikar:packages/core/src/kernel3d/maclado-gap.ts`: the chain's complement is ONE pinched region, cut by a documented rule (§6 status, same as the walk): spherical Delaunay of wheel centres (brute-force convex hull of unit directions — every point extreme on the sphere), bridges between mutually nearest rim vertices, one tile per hull triangle; rim runs pick the direction whose midpoint faces the triangle centroid; zero-length bridges at welded pinches deduped. Cut verified HARD because the verdict is negative: hull count 2W−4=34 exactly, partition residual ~1e-9 mm² of 30,772.81 mm², every rim vertex covered. **THE VERDICT (§9.1+§9.2 closed for this rule): 34 tiles → 33 congruence classes at maker tol (1e-3mm/1e-4rad), STILL 33 at 1mm/0.1rad — three decades robust; collapses only at absurd tol to 6 = ring-size bucket count (no information, pinned as by-design test). CAUSE IS THE FIELD NOT THE CUT: 51 hull edges take 32 distinct centre distances (43.78=joinDist…76.48mm) — the greedy walk yields a CONTINUUM of separations, so NO cutting rule could produce a small filler vocabulary. K10 transfer condition recorded: a successor placement rule must QUANTIZE its separations by construction.** Also pinned: area-compensated distortion (grow tile 15% + bisect-shrink another) keeps Σ areas exact to sub-µm² while per-tile congruence fails — aggregate cannot accept tiles (M3 lesson at gap scale). 9 tests in `bikar:packages/core/tests/kernel3d/maclado-gap.test.ts`. Doc: status line → v3 (K7: status must agree with body), M4b bullet records step 2, D-030 in decisions log; open follow-on = USER SCOPE DECISION (quantized-separation rule: discrete step words, lattice-guided walks — survey does not promise one exists). Mechanics: eslint complexity 16 → extracted `orientedHullFace`; pointer gate needs primary `~/Workspace/git/bikar` PULLED (not just fetched) to resolve new files; map re-pin was clean (merge only added files, no anchor drift). [[pr-flow-for-all-repos]]

**2026-08-08 (#116 M4b step 1) — the spiral-chain placement search SHIPPED both repos (bikar PR #90, squash `856db18`; 3d-models PR #63, squash `f8bbcda`).** New `bikar:packages/core/src/kernel3d/maclado-spiral.ts`: `placeSpiralChain` (greedy advancing chain: wheel 0 at +z; each wheel's tip 0 aims back along the chain; candidate tips k=1..n−1 in CCW order, sharpest turn first; first cap not overlapping any placed wheel wins; frame math `stepFrom`/`frameFacing` makes both facing tips lift to the geodesic midpoint → EXACT join, residual ~1e-14 mm, still run through `checkWheelJoin`), `chainCoverage` (deterministic Fibonacci sampling), `chainAreaReport` (reflex-safe Girard star-rim areas). `placeWheelInFrame` extracted public from maclado-field.ts so search frames lift through the M4 code path. **The DOF argument, now in the doc's M4b bullet:** wheel = 3 DOF, exact join = 3 constraints, covering field needs J ≥ 3W/2 → generically overconstrained; M4 (90 constraints on 61 DOF) closes only by icosahedral degeneracy; an asymmetric exactly-joined subgraph must be a CHAIN (J=W−1), all closure burden on fillers. **Pinned honest numbers:** at M4's own θ ≈ 20.905° the chain places **19 wheels — one FEWER than the icosahedral 20** (greedy loses to symmetry at symmetry's angle; what it buys is θ as a free knob: 38 wheels at 15°); tip word `023434535455456557`; **zero incidental welds + zero exact-touch near misses at every probed angle** → windings never re-touch, so M4b fillers have FREE EDGES, not welds; gap ≈ 68% of sphere by star-rim area. By-design FAIL: half-step spin keeps centreDistanceOk while joined=false. §9.1 rewritten: convergence risk now lives ENTIRELY in whether the inter-winding gap decomposes into a small congruence-class set of whole fillers. Mechanics: eslint complexity 11→extracted `scanIncidentalContacts`; the use-case `--refresh` to `856db18` surfaced 3 anchors shifted +14 by the extraction (buildMacladoField L284, macladoSeamGraph L340, solidifyMacladoField L447) — re-seated by hand. **NEXT (M4b remaining): extract inter-winding gap polygons from the chain (seam-graph faces or rim-arc walk), cluster by §5.3 congruence (sweep the tolerance knob, §9 item 2), then whole-filler verdict or documented partial per §8's gate.** [[pr-flow-for-all-repos]]

**2026-08-08 (#116 M5) — maclado M5 print+gallery SHIPPED both repos (bikar PR #89, squash `eb4f19c`; 3d-models PR #62, squash `83d67d3`).** DSL surface, deliberately thin: `base wheelfield` + `wheel points <n> [contact <x>]` (default `{points: 9}`) + `place rule dodecahedral`; `inscribe`/`project faceted`/`subdivide` REFUSED for Family 3; provenance reuses dodecahedron-base + `spherical` + family `'lattice'|'weave'` so ZERO CLI changes — 17 new tests incl. both presets end-to-end. Published presets `bikar/patterns/Orbs/{Maclado-9,Maclado-9-Weave}.bkr` (index.json 109 entries, public-surface count 109 + why-entry — the three-place registration rule). Pinned M5 numbers: solid genus 379, χ=−756, watertight, 9,312 tris, 86.8 cm³ @ R=60; weave χ=0, 46 strands, 390 crossings, 7,200 tris, 8.6 cm³; views emit 3 axis files; `--check print` solid=PASS w/ F7 only (3.78 mm² first-layer → brim), weave=F3 (mid-air ribbon spans near top pole → supports) + F7. 3d-models #62: two gallery entries (no `lab:` — Orb Lab has no maclado preset; template tolerates absence), `make orbs` needed NO Makefile change (glob picks up presets; constraint was primary bikar checkout on merged main with built dist), UC22 in use-case map (re-pin + 20 drifted anchors), P8 print sheet stating §7's ~0.4 mm-nozzle K10 transfer condition + both print-check findings, design doc §8 M5 done + Appendix A as-shipped note (4 diffs from sketch; spiral → M4b). Gate lessons: (1) counts gate caught backlog's catalog-entries 30→31, and C4 means the LIST beside each count names P8 too; (2) NEW-to-me site-graph gate (`docs/site-graph.json`, hook 40) pins index.html line anchors — gallery insertions moved 'Open in Lab' L532→538, re-pin by hand; (3) bikar CI's pointer gate rejects a bare `docs/...` path for a 3d-models doc — write `3d-models/docs/maclado-orb-design.md` (architecture.md:177 precedent); (4) CLI output flag is `-o`, `--out` dumps binary STL to stdout and `--help` is unsupported. **NEXT: M4b — the asymmetric faithful field** (spiral-heuristic placement, whole small fillers by SEARCH not symmetry; §9.1's convergence risk lives there; `place rule` statement designed to extend; may honestly end at a documented partial per §8's decision-point language). [[pr-flow-for-all-repos]]

**2026-08-08 (#116 M4) — the full maclado field MERGED (bikar PR #88, squash `d20e3f5`) as the SYMMETRIC dodecahedral field, and the design doc's §2 was corrected by M4's own construction (3d-models PR #61, squash `ae264ce`).** User's AskUserQuestion decision = **"Staged: symmetric first"**: M4 = 20 wheels on the dodecahedron vertices; the asymmetric faithful field is now explicit milestone **M4b**. The core result is the **divisor trick**: a 9-fold wheel is C₃-invariant (3 | 9), so it sits on an icosahedral 3-fold axis — the ONLY option (5∤9, 2∤9) — and 120 = 3·40 aligns one spin's tips at all 3 neighbours simultaneously → 30 exact tip-to-tip joins + 12 congruent 30-gon fillers, whole BY SYMMETRY, globally icosahedral. This DISPROVED doc §2 v1's "no global rotation relates ≥2 nine-fold centres" (a K1 over-hardening of the true theorem "no polyhedral 9-fold axis"); the doc records the defect in place. K2 hedge kept: Kaplan plays the same trick at full site order (10-stars at 5-fold sites); no surveyed source plays it with a PROPER divisor. New `bikar:packages/core/src/kernel3d/maclado-field.ts` (buildMacladoField/macladoSeamGraph/solidifyMacladoField) + tests. Pinned numbers: capHalfAngle = acos(√5/3)/2 ≈ 20.905°; tiling identity 20·A_wheel+12·A_filler=4πR² at 9e-15; fillers have 10 reflex vertices → plates must be EARCUT via gnomonic chart, NOT centroid-fanned (own guard threw); seam graph 510 nodes/900 edges/390 deg-4+120 deg-2; weave watertight, 390 crossings, 46 strands; solid watertight **genus 379** (closed shell with H=380 tunnels has genus H−1, χ=4−2H=−756 — M1's cap read genus=holes only because a capped disc is one sphere); omitFiller→not watertight (by-design FAIL); orbit test constructs the 20 rotations and maps the 180-tip set onto itself. strutWidth 2 in tests (3 degenerates inner insets at R=60). Doc v2 (PR #61): §2 corrected in place, §1 goal-2 + star-ball non-goal fixed, §8 M1–M4 marked done + M4b added (where §9.1's convergence risk actually lives), §9.3 parity proven for arbitrary welded seam graphs (tangent-kiss refusal tested). Pointer-gate mechanic: `bikar:` pointers resolve against the sibling PRIMARY checkout — `git -C ~/Workspace/git/bikar fetch origin` was required before `make validate-pointers` saw d20e3f5's files. **NEXT: M5 (print + gallery — .bkr source, likely needs DSL wiring per Appendix A; STL through mesh gate; gallery entry; use-case row; print sheet), then M4b.** [[pr-flow-for-all-repos]]

**2026-08-08 (#116 M3) — maclado filler-closure validator + patch-first three-wheel probe MERGED (bikar PR #87, squash `9352f76`).** User's genuine AskUserQuestion decision = **"A — Patch-first probe"** (over B full-sphere spiral, C checkpoint gallery-card) after a visual explainer artifact (`scratchpad/maclado-m3-options.html`, favicon ✳️) drew the real rosette geometry for each option. New `packages/core/src/kernel3d/maclado-filler.ts` = §5.3's PER-FILLER congruence validator: `fillerMetrics(polygon,radius)` (geodesic edges = radius·angleBetween3, interior angles via tangent-plane projection acos(clamp(dot(tPrev,tNext))), Girard area R²·(Σθ−(n−2)π); throws <3 verts); `fillerCongruence(polygon,template,radius,opts)` (best cyclic-rotation align of BOTH forward and reversed ring — reflection-invariant — congruent iff edgeResidual≤1e-3 mm && angleResidual≤1e-4 rad); `checkFillerClosure(fillers,template,radius)` → `{accepted: perTile.every(congruent), count, areaSum, perTile}`; `scaleFillerAboutCentroid(polygon,factor,radius)` (slerp each vertex `factor`× its angular distance from the centroid dir — monotone in factor, for area-compensating stretch). Cluster geometry in `maclado-wheel.ts`: `clusterColatitudeDeg(capHalfAngleDeg)` solves cos²ρ=(2·cos2θ+1)/3 (θ=30°→ρ=35.264°, throws if cosSq∉(0,1]); `placeMacladoTriple(wheel,params)` places 3 caps at colatitude ρ, 120° apart, returns `{wheels, filler}` where filler = the 3 innermost rim tips bounding the central gap. **Patch-first FINDING (positive): the symmetric local cluster's 3-fold symmetry FORCES the filler equilateral (whole) — verified live: edges 9.5452 mm, angles 60.21°, area 39.577 mm².** Load-bearing FAIL test (test 4): grow one filler ×1.15, binary-search a shrunk filler to exactly compensate area → distorted set has count===3 AND areaSum≈3·unit (6 dp) BOTH satisfied, yet accepted===false, perTile [false,false,true] — the aggregate reads "closed", only per-tile congruence fires (lego-lab §14's one-reversed-triangle case). Test 5 makes it explicit: `naiveAccept`(count+area) ===true while report.accepted===false. **Full-sphere closure stays the OPEN §9.1 risk deferred to M4** — the probe proved the LOCAL symmetric patch closes whole, not the field. All gates green (15 maclado tests, 682 kernel3d, full `npm run ci` exit 0, eslint complexity ≤10, prettier, doc-pointers/jsdoc/decisions/bundle). NEXT: merge #87, then M4 (full field + ribbon parity — weave.ts accepts only degree-2/4 nodes but maclado seams can be degree-6). [[pr-flow-for-all-repos]]

**2026-08-08 (#116 M1+M2) — maclado orb implementation STARTED; M1 and M2 both merged to bikar origin/main.** Worktree `~/Workspace/git/bikar-maclado` (off origin/main; isolated so it doesn't disturb the parallel session's `feat/studio-labs-home` checkout or the stale `main` in bikar-lego-lab). All code in `packages/core/src/kernel3d/maclado-wheel.ts` + barrel `kernel3d/index.ts`. **M1 (bikar PR #85, squash `1f27cfd`):** `generateMacladoWheel` (tips O_k at 100·(k·2π/9), valleys V_k at contact·100 on the half-step, core I_k at coreFraction·100; voids = core n-gon + per-k inner triangle + petal kite; boundary alternates tip/valley = 2n pts; DEFAULT_CONTACT=0.5, DEFAULT_CORE_FRACTION=0.32), `checkWheelClosure` (§5.1 angular-closure validator — rotate each outer point by 2π/n, must land on next within tol), `solidifyWheelCap` (lift flat tiling onto spherical cap, inset each void by strutWidth/2 via `insetPolygon` which THROWS on collapse, emit top/bottom/holewall bands + rim wall, meshStats-gated). Load-bearing FAIL test: 9 points on a 2π/8 step — right count, wrong step, only closure catches it. **M2 (bikar PR #86, squash-merged):** `placeWheel` (lift rim onto north-pole cap, orient by `WheelPlacement`{spinDeg about cap axis, then tiltDeg about +y}), `placeAdjacentPair(wheel,params,'joined'|'phaseBroken')` (both tilt 2·capHalfAngle so centre geodesic distance is IDENTICAL + spin 180° so a tip faces back; joined lands tip-on-tip exactly, phaseBroken adds half-step spin so a valley faces back and nothing coincides), `checkWheelJoin` (§5.2 — computes BOTH aggregate centre distance AND per-rim-point pairwise coincidence, gates on the latter). Load-bearing FAIL: phaseBroken passes the distance check yet pairs 0 contact points — aggregate can't discharge a per-contact-point claim. Geometry note: a pattern tip (flat radius 100) sits at cap half-angle exactly, so two caps tilted 2θ apart meet tip-to-tip on the great circle between centres; tip-to-tip exact coincidence needed a 180° spin (a tip, not the valley at azimuth π, faces back for odd n). Imports `frame.ts` for rotate3/angleBetween3 (no cycle). Mechanics: `gh pr merge --delete-branch` fails locally ("main used by worktree bikar-lego-lab") but the REMOTE merge succeeds — verify via `gh pr view --json state`; branch off `origin/main` fresh for each milestone (squash breaks ancestry). **NEXT: M3 — the filler-closure solver, the doc's flagged DECISION POINT** (§8: "if no placement rule the survey permits yields whole fillers with a closable ribbon, the honest outcome is to record that in the decisions log and stop at a documented partial, not ship a distorted-filler orb"). M3 needs: a multi-wheel placement leaving a filler gap, filler-region extraction, a canonical filler template set, and §5.3's PER-FILLER congruence validator (each filler's every edge+angle matches a canonical template — an aggregate area/count cannot see one stretched tile). Then M4 (full field + ribbon parity, needs every welded node degree-2/4 or the parity solver generalised past degree-4 — §5.4) → M5 (print + gallery). [[pr-flow-for-all-repos]]

**2026-08-08 (#115) — the 9-spike "maclado" ribbon-orb DESIGN DOC + research MERGED (3d-models PR #60, squash `4f6bd45`).** From IMG_1124.png: Ángel María Martín López's 9-pointed maclado sphere (9-fold rosettes joined by whole filler tiles, global symmetry deliberately broken). User's genuine AskUserQuestion decision = **"Go straight for faithful 9-maclado"** — a NEW construction family (Family 3), highest fidelity/highest risk, NOT a symmetric star-ball substitute. `docs/maclado-orb-design.md` (DRAFT v1, pre-implementation) + `docs/research/maclado-orb-survey.md` (399-line survey, provenance header, 13-source fetch record, excluded from doc_pointers/D4). **Load-bearing theorem (§2):** the finite rotation groups of the sphere admit polyhedral site-symmetry orders of only 2/3/4/5 — never 9 — so ≥2 equivalent 9-fold centres CANNOT be related by any global rotation (bar trivial single-axis beach-ball); symmetry-breaking is FORCED, not stylistic (*maclado* = crystal twinning). **§3, verified firsthand vs bikar origin/main e8b07d5:** the current `orb` engine structurally REFUSES 9-fold via three coupled commitments (Platonic base, one-shared-pattern-per-face, manifold-or-nothing) → needs Family 3 = free wheel placement + rim-overlap welding + filler-closure solver + globally-consistent ribbon parity; §5 ships per-stage `**Validator:**` with the hard by-design FAIL. **Honesty carried (K1/K2/K7/K10, tightened by an adversarial grounding audit before merge):** the maker's exact placement rule is NOT retrievable (Facebook not indexed; indexed record is ten-fold/regular), so Family 3 is validated by the PROPERTY his object has (every filler whole, every ribbon closed, one watertight manifold), NOT by matching his object. The reused manifold gate is COMBINATORIAL and does NOT catch interpenetrating bands (the woven family exploits exactly this — interpenetrating tori pass) → a NEW self-intersection check is flagged, not assumed (§5.5); the parity solver is face-coupled + private + throws on non-degree-2/4 nodes, so "reused unchanged" was corrected to "reused with new work". **Next: #116 implement in bikar (branch→PR→merge bikar first, then .bkr + gallery + use-case map in 3d-models), milestones M1 one wheel (numeric nonagon θ=2πk/9 + angular-closure validator) → M2 placement/overlap → M3 filler-closure solver (the hard part; documented decision-point — may stop at a documented partial) → M4 full field + ribbon parity → M5 print+gallery.** [[pr-flow-for-all-repos]]

**2026-08-08 (#110/#111/#112, D-029) — a THIRD `--check` strength (the colour set) + a GPU-free catalog gate, split at the GPU.** Closes the gap D-028 left: counts are pure geometry and don't move when colours collapse, and goldens don't port (K10), so on a backend whose goldens were never baked a model can regress to the D-026 all-grey blob and pass BOTH gates. The middle gate classifies every foreground pixel to the nearest colour in **the model's own palette + background** (not exact pixels, not the whole LDConfig set — that's what makes a shaded blue still read blue and so PORT across backends), accumulates best coverage per colour across the angle set, and compares the colours clearing `--colour-min-area` (default 0.01) as a SET (`setsEqual`) against a committed `visibleColours`. **The load-bearing trap: the expectation is the VISIBLE set, deliberately ⊊ resolved colours** — Brick-Stack resolves 4 but the lower brick's yellow studs are occluded in every angle → `visibleColours`=3; so `visibleColours` is BAKED by `--update-goldens` from a trusted backend (same discipline as golden PNGs), never auto-derived (which would false-fail occlusion). Landing it exposed+fixed a latent read-back bug: three's LDrawLoader gives a multi-colour brick a material ARRAY, and the old single-material read reported an empty palette for every multi-colour model. **Hook boundary (2nd AskUserQuestion decision) = catalog well-formedness ONLY**: the render keeps a GPU so it stays skill-invoked, but `bikar:scripts/thumbnail-catalog.test.mjs` reads files and never renders — every `.mpd` paired with an `expected.json`+`.notes.md` (none orphaned), every resolved colour named in that model's notes (W-F1 coherence ported), catalog non-empty — so it runs in CI (`test:scripts`) AND `bikar:.husky/pre-commit` (trigger includes `--diff-filter=D` so a stranded expected.json is caught as the orphan). Per-model `<name>.notes.md` = the human half of the checklist (colour legend + occlusion reason + per-angle purpose). The whole split is ONE axis: **does a GPU sit in the path?** counts+colour port→both in `--check`; golden pixels don't→soft; catalog has no pixels→the only hook. bikar **PR #83 (`6dba045`)** = colour gate + `visibleColours` in `scripts/thumbnail-gate.ts` + the multi-material read-back fix in `packages/lab/src/ldraw-readback.ts` (which moved 3 UC19 anchors: L340→369, L339→368, L203→221). bikar **PR #84 (`e8b07d5`)** = Brick-Stack.notes.md + thumbnail-catalog.test.mjs (`catalogViolations` pure, 3 real + 5 witnesses) + the husky block. 3d-models **PR #59 (squash `17e41f6`)** = lego-lab-design §16 (two D2 Validator/PASS/FAIL, FAILs = grey-blob + unnamed-colour orphan) + D-029 + `validate-render/SKILL.md` (now "gated three ways", K7) + use-case map re-pin. Mechanics: merged both bikar PRs to origin/main BEFORE the 3d-models commit so §16's `bikar:` pointers resolve; 3d-models has NO CI workflows (gates are pre-commit only), so a MERGEABLE/CLEAN PR with no checks merges straight away. [[3d-models-use-case-hook]] [[pr-flow-for-all-repos]]

**2026-08-07 (#106/#108/#109, D-028) — the LDraw thumbnail CLI + `validate-render` skill shipped; a `.mpd` renders to a *set* of camera-angle PNGs, gated two unequal ways.** Answered the original ask ("a CLI to go from .mpd→png with MULTIPLE camera angles + an accompanying validation skill"). Backend chosen via AskUserQuestion: **Playwright full Chromium** (`channel:'chromium'`), not the default headless shell — research §10.3 measured the shell failing to create a WebGL context on this machine class while full Chromium reaches the GPU through ANGLE/Metal (`npx playwright install chromium` once). Validation basis: **counts AND golden pixels, both**. Skill-now/gate-later (D-028) per the "no skill/gate before measured recurrence" precedent. The load-bearing idea (research §10.5): **on a file with no edge lines a render is evidence of shape, not structure** — two stacked 2×4 bricks are pixel-identical to one six-plate block, the seam is never drawn — so the honest output is a *set* of angles beside the numbers, and the number is the real gate. **Two deliberately unequal gates:** counts = HARD (the whole read-back — placements/meshes/tris/part hashes/winding/edge-pairing — deep-compared vs `<name>.expected.json`, fails on ANY drift; an aggregate cannot discharge a per-part claim); golden pixels = SOFT (`--tolerance` default 0.02, GPU/driver dependent, size-mismatch→hard fail). **K10 transfer condition:** only the counts port across backends — the goldens are valid ONLY for the backend that baked them (this machine's chromium-on-darwin via ANGLE/Metal); elsewhere, run `--check` for counts and treat a pixel fail as "re-baseline here", not "model broke". The CLI renders the SAME `ldraw-scene.ts` the studio §14.4 read-back panel shows (`thumbnail.html`), so a thumbnail is the same brick the panel reads back, not a second renderer that could disagree. bikar **PR #82 (squash `1113052`, on origin/main)** = `scripts/render-ldraw-thumbnails.ts` (npm `thumbnail`, `--check`/`--angles`/`--update-goldens`/`--tolerance`) + `scripts/thumbnail-gate.ts` (`countsMatch`/`pixelDiffRatio`) + `scripts/thumbnail-gate.test.mjs` (9 witnesses, incl. the load-bearing count-fail-while-pixels-pass case) + `packages/lab/src/ldraw-scene.ts` (LDRAW_UP_FLIP moved here from ldraw-preview.ts) + `packages/lab/thumbnail.html` + `packages/lab/src/thumbnail-page.ts` + Brick-Stack.{mpd,expected.json,iso/front/below.png} fixtures. 3d-models **PR #57 (`3190f22`)** = lego-lab-design §15 (with D2 Validator/PASS/FAIL where FAIL is count-drift-while-pixels-pass) + D-028 + use-case map re-pin (UC19 ldraw-preview→ldraw-scene, 6 line-drifts + as_of bumps by hand — `--refresh` re-pins hashes only, NOT line numbers). 3d-models **PR #58 (`d361785`)** = `.claude/skills/validate-render/SKILL.md`. Catalog test in `packages/lab` excludes `thumbnail.html` from PAGES via `NON_PAGE_HTML` (headless render target, not a visitable studio page) with a guard test that it never becomes a Rollup input. Mechanics: merged bikar #82 to origin/main BEFORE committing 3d-models so §15's `bikar:` doc-pointers would resolve; `gh pr merge --delete-branch` failed locally ("main used by worktree bikar-lego-lab") but the REMOTE merge succeeded — verify via `gh pr view --json state,mergedAt`, don't retry. [[3d-models-use-case-hook]] [[check-for-existing-e2e-before-reporting-blocked]]

**2026-08-06 (#104, D-026) — per-part LDraw colour is a real DSL feature; an assembly's parts export in distinct colours.** Root problem: model-viewer showed the whole Brick-Stack as one grey blob because the MPD emitter wrote `LDRAW_DEFAULT_COLOUR = 7` on every type-1 placement line. Chosen via AskUserQuestion: **"Codes + grounded names"** — a `place` clause accepts either a bare LDraw code (0–511, asserts nothing) OR a grounded colour name. Names are grounded, not invented: fetched `LDConfig.ldr` (`https://library.ldraw.org/library/official/LDConfig.ldr`, header UPDATE 2026-05-29, fetched 2026-08-06) and every DSL name is the LDConfig name lower-cased (black/blue/green/red/light_grey/yellow/white/light_bluish_grey/dark_bluish_grey/dark_grey), no synonym. Colour rides the placement line, not the part def; inline geometry stays 16. Resolution is deferred to eval-time so a bad name fails every format. All-grey emission stays byte-identical (default still 7). bikar PR #79 (squash `2d60b00`) = LDRAW_COLOUR_NAMES/resolveLdrawColour + AssemblyPlaceNode + parseOptionalPlaceColour + PlacedPart.ldrawColour + Brick-Stack.bkr (`place Base color blue`/`place Cap color red`). 3d-models `03facc4` (pushed to master) = research §7.4 grounded palette table + S15 LDConfig fetch row, D-026, lego-lab §14.5, use-case map UC18 extended with the clause + palette pointers. **Grounding was a prerequisite, not decoration: the emitter's doc comment cites "§7", so shipping the code before fetching LDConfig would have made that pointer aim at ungrounded content (a K9/pointer hazard).** Use-case map mechanics that bit: `--refresh` pins bikar to origin/HEAD, which was STALE (local ref predated my merge) so it first pinned to a commit lacking resolveLdrawColour — `git fetch origin main` first, THEN `--refresh`; advancing the pin to 2d60b00 surfaced 11 drifted bikar anchors (mostly pre-existing) that had to be re-seated. **Process note: `03facc4` went DIRECTLY to master, not via a PR (the bikar half did go through PR #79). Flagged to user 2026-08-06; user chose "Leave it, PR next time" — accept this one, route ALL future 3d-models changes (docs included) through PRs. Recorded as [[pr-flow-for-all-repos]].** Worktree cleanup done 2026-08-06: verified `feat/assembly-place-colour` fully merged (file-restricted diff origin/main vs branch empty), detached `bikar-tile-border` onto merged `2d60b00` and `git branch -D`'d the branch, preserving its untracked `scratch/` (26 Aug-4 text-emit study files). All three bikar worktrees now on merged content (primary detached 60383b5, bikar-lego-lab main, bikar-tile-border detached 2d60b00).

**2026-08-06 (#105, D-027) — per-BRICK STUD colour shipped and merged both repos; #104's per-part colour now composes with a second, independent stud colour.** `place <Piece> [color <c>] [studs <c>]` paints the pins apart from the body. The colour CANNOT ride the type-1 placement line (one inherit slot, colour 16), so the emitter fixes the stud code onto the **stud triangles inside the inline block** while body faces stay 16 and inherit the placement colour — the *mixed-colour inline part*, spiked before implementation (three.js LDrawLoader read back a blue body + red body sharing one yellow-studded part). A stud is identified EXACTLY, not heuristically: a triangle is a stud iff a vertex is above body height H (`isStudTriangle`, z > H+1e-3); `stackBrickSlabs` puts studs in the one slab above H and nothing else, so the top face at exactly H never trips it — the K10 transfer condition, written in the emitter + §14.6. De-dup splits exactly on a stud-colour difference because the stud code lives in the geometry-text block key. Emitter REFUSES `studs` on an empty stud set ("no geometry stands above its top face"). Also closed the read-back panel's magenta gap (robustness>ease): §14.4 preloaded only code 7, so any grounded body/stud colour drew magenta — now preloads the full ten-colour grounded LDConfig palette (§7), retroactively fixing #104's coloured bodies in the panel too. bikar PR #80 (squash `0f497df`) = parser `parseOptionalPlaceColours` + AST studsColour + evaluator resolvePlaceColour/ldrawStudsColour + cli LDrawPlacement.studsColour + emitter isStudTriangle/blockGeometry + ldraw-readback ten-colour palette + Brick-Stack.bkr (Base blue/yellow, Cap red/green); 2586 pass|3 expected-fail, ci green. 3d-models PR #56 (squash `2b9e8a9`, VIA PR per [[pr-flow-for-all-repos]]) = §14.6 (with Validator PASS/FAIL — note the gate wants a bare `PASS:`/`FAIL:` at line start, NOT backticked `` `PASS:` ``), D-027, UC18 prose+pointers, bikar as_of bumped 2d60b00→0f497df with 15 drifted anchors re-seated. Use-case `--refresh` re-pins hashes and REPORTS anchor drift but does NOT auto-move line numbers (that would silently rewrite a claim) — apply the reported new lines by hand; `validate.py --full` then 145 pointers all valid. Both worktrees clean; bikar-tile-border detached at 0f497df, its untracked scratch/+build/ preserved.

**2026-08-05 (#103, D-025) — the confusability check is WIRED and BLOCKS; text-emit T-series is now fully closed.** The two checks that shipped in D-023 but were never called — `checkLabelCharset` (one label mixing the slashed `0` and capital `O`) and `checkLabelSetCharset` (two labels on one part that fold to the same string, e.g. `O3`+`03`) — are now in the CLI mesh-gate label loop beside gap+counter. **Policy chosen via AskUserQuestion: BLOCK** (FAIL under `--check`, no STL written), over *warn* (a confusable plate would ship silently) and *compile-time refuse* (would make charset stricter than its siblings for no reason). Both run because neither catches the other's case: the set check fires once over the piece's whole label set. Rationale that made block cheap: `CONFUSABLE_PAIRS` is deliberately just `['0','O']` (1/l/I excluded, measured apart) so false alarms ≈ 0, and the fix is a rename the author picks — auto-substitution would make the check unfalsifiable (the §7 Q2 argument). bikar PR #78 (squash `29813b8`) added the wiring + 2 by-design failing witnesses to `label-gate.test.ts` (`MC-2 PORT0`, `O3`/`03` plate). 3d-models PR #54 (squash `aaf4da6`) recorded D-025 (matching D-023/D-024 format), documented the checks in text-emit §5, and narrowed §7 Q2 to the still-open gap/tracking arm with a pointer to D-025 (K7 self-consistency). **Mechanics lesson worth keeping: a `../../bikar/...` markdown link in a 3d-models doc is checked by the docs gate's D1 (resolve-on-disk) against the PRIMARY bikar checkout, which is parked on a stale HEAD — so links to NEWER bikar files (label-gate.test.ts, text-layout.ts) would FAIL D1. Cite bikar via the PR URL + bare backticked paths instead (what text-emit-design.md already does with `[#74]`).** Use-case map: bikar as_of STILL parked at `3a759b5` (bumping → the same 8 unrelated bikar anchor drifts). Text-emit is done end-to-end: T0 #97, T1 #98, T2 #99, T3 #100, #101 (bet→coupon→catalog gate declined = D-024), #102 (slashed zero = D-023), #103 (charset = D-025) all merged.

**2026-08-05 (T3) — the machine card's four flat plates are LABELLED and both repos merged.** bikar PR #77 (squash `42c3344`) added four `text` statements to `patterns/Coupons/Machine-Card.bkr`: MC1BoreSweep→`MC-1 BORE`, MC1FitLadder→`MC-1 FIT`, MC3BridgePlate→`MC-3`, MC5WarpPlate→`MC-5`. The emitter is extrude-only, so of the card's 26 pieces only the 4 flat plates take a label; the 14 rods / 7 tubes / 1 revolve keep filename+size-ordering+bagging — and those are exactly the coupons that self-identify by size, so the extrude-only limit happens to cover the anonymous pieces. Placement is off-measurand (K10): MC-5's label is 0.4 mm deep, bottom-edge-centred so it perturbs warp corners C/D equally; MC-3's sits at the top edge clear of the blind-bore line so it never thins a bridging ceiling. Each clears §5's gap+counter gate in the shipping face (tightest MC-5 cap 5 = 0.060 mm ≥ 0.4 floor). 3d-models PR #52 (squash `0da5873`) recorded it: §3.2 rewritten, §7 tris updated (MC1BoreSweep 1572→2440, MC1FitLadder 1312→1776, MC3BridgePlate 2076→2596, MC5WarpPlate 272→720 — euler/watertight/degenerate/minFeature/volume and the 89.7 cm³ total ALL unchanged, blind pocket + relief excluded from minFeature), §8 weakness bullet flipped, text-emit §8 T3 milestone marked done. **Two mechanics facts worth keeping: (1) `make coupons` uses `BIKAR_DIR` (default `~/Workspace/git/bikar`, the primary checkout parked on a stale branch); override `BIKAR_DIR=~/Workspace/git/bikar-tile-border` after `npm run -w @naqshcoffee/bikar-cli build` to render against a worktree at merged main — verified all 23 rungs match §7. (2) The starter-pattern bundle reads `patterns/**/*.bkr` verbatim via `import.meta.glob('?raw')` at bundle time, so editing a `.bkr` needs NO regeneration — `starter-patterns.test.ts` reads the same disk file. Use-case map: re-pinned 3d-models as_of to HEAD but LEFT bikar at `3a759b5` deliberately — bumping to 42c3344 (what `--refresh` does) drags in 8 unrelated bikar anchor drifts (evaluator.ts/index.ts/solidify-piece.ts moved), which are a separate maintenance task, not this doc PR's.** Remaining text-emit work: #103 wire Charset (confusable block-vs-warn-vs-refuse still open; gap+counter already in --check), #101 evaluate a bet→coupon→catalog gate before writing one.

**2026-08-05 — text-emit slashed-zero re-bake SHIPPED and merged both repos.** bikar PR #75 (squash `3a759b5`) re-baked the whole Source Code Pro Bold face from **2.042** (not the 1.017 on this machine, whose `zero` OpenType feature is empty so `zero.a` is unreachable): shipping `0` is now the slashed `zero.a` — one ink island, two sibling lobes at depth [0,1,1], no dot, `labelMinGap` returns null so the counter check (0.959/0.960 mm ≥ 0.4) is load-bearing. B2's only depth-2 witness moved to `packages/core/tests/kernel3d/fixtures/glyphs-dotted-zero.ts` (baked `--only 0` from the same file) rather than vanishing. `bake-glyphs.py` gained `--alt CHAR=GLYPHNAME`. 3d-models PR #51 (squash `3436f91`) corrected §5–§6 literals to 2.042 (LG-B2 0.877, WWW 0.060, thinnest stem 0.580), added D-023, and kept the 1.017 numbers verbatim under a dated addendum in `outline-font-emit.md` (version-supersession, NOT a D4 withdrawal). **Two gate lessons worth keeping: (1) a git trailer's continuation lines must be INDENTED — 30e2aa2's multi-line `Calibration-Baseline-May-Grow` value wrapped at column 0, so `git log --format='%(trailers:key=…)'` returned empty and CI never set `may_grow=1`; the fix is a single-line trailer on the tip commit (which is also what the gate's own `git log -1` guidance checks). (2) The 3d-models doc-pointer gate resolves a sibling path only at a git REF, so a new bikar fixture stays unresolved until #75 merges AND `git fetch origin` runs in the shared `~/Workspace/git/bikar` checkout.** Remaining text-emit work: #99 T2 (solidifyText + `text` statement + validators into mesh gate, in_progress), #103 wire checkLabelGap/Counter/Charset, #100 T3 label 23 coupons + reprint machine card, #101 evaluate a bet→coupon→catalog gate. [[3d-models-use-case-hook]]

**2026-08-02 (latest) — `footprint outline` SHIPPED + LG-B2 Rosette-Brick.bkr AUTHORED (bikar `main` `bf6c602`, 3d-models `master` `992d795`, both pushed; full bikar CI green). This supersedes the "pending user green-light" / "cannot be authored" notes below — Task #11 is DONE.** The DSL gained `footprint outline` as the third footprint arm (`outline` is a contextual identifier, zero new reserved words; requires `inscribe`; stud grid derived exactly as `auto` — "outline changes the shape, never the lattice"); body ring = outer ring of the single edge-connected face-union component (V19 hard-errors naming the count otherwise), recentred by `center − art.center`; V18 measured-cavity-wall gate (every vertex + edge midpoint ≥ wall − SNAP inside the body, ≥3 verts, winding preserved) refuses cusps sharper than ≈23.1° — star cusp refused / decagon builds are the pinned tests. The design audit's F5 hole was real and fixed: `mintDeclaredPorts` hard-coded `rectangularBody` and now takes `spec.bodyOutline ?? rectangularBody(...)`. Decision doc `bikar:docs/decisions/2026-08-02-pattern-outline-footprint.md` (tag `brick-outline-footprint`, option A of four; §0 premise check records the F5 hole honestly). Rosette-Brick measured: 6×5 grid, six tube anchors in eight fully-covered cells, watertight, max radius 20.00 mm (the catalog's earlier "two tubes" sketch was corrected — the build is the authority). Registration lesson (bikar #67's framing): a new preset is claimed in THREE places — lab pin-list test, `patterns/index.json` manifest, `public-surface.json` count (now 106, with a provenance paragraph). 3d-models follow-through in `992d795`: pointer-baseline shrank by the three forward references, catalog LG-B2 → "model authored", backlog row + note-10 resolved, design-doc status line + one honest §6 divergence (no grammar §12 row — the coverage table keys on dispatched keywords only), use-case map re-pinned (4 drifted anchors repaired; 3d-models as_of deliberately pinned to master-reachable `241121b`, not the branch-only `479d77f` from the un-merged `fix/k7-validator-shape` K7-doc branch). CAL-ANC-01/CAL-INW-01 stay provisional until LG-B2 prints (gated on LG-F1/LG-B1; printing HELD pending a printer). (3d-models `241121b`, pushed; supersedes the "pending user green-light" note below for the *design*; the bikar *implementation* still needs the green-light).** `docs/pattern-outline-brick-design.md` v2 specifies `footprint outline` (third arm of the footprint mode, zero new reserved words) with two verbatim research surveys, a C4 grounding audit (findings F1–F7 applied), and CAL-ANC-01/CAL-INW-01 registered in bikar (`1b7770d`, pushed; both settle by LG-B2). Load-bearing measured facts: (1) the relief path unions **all** bounded faces — a rosette's union is the fully tiled disc, so all 8 corpus rosette sources = exactly 1 edge-connected component with sharpest convex cusp **72°**; (2) `insetRing`'s miter clamp floor is `2·arcsin(0.2) ≈ 23.07°` (cos var = sin²(θ/2), brick.ts:773) — outline mode's V18 precondition, which the corpus clears 3×, so Rosette-Brick.bkr is authorable under v1 as specced; (3) `chainBoundaryRings` (evaluator.ts:1280) already hard-errors on point-pinches, and vertex-touching faces split into separate components (grouping is edge-based). Audit's F5: declared-port minting at evaluator.ts:2409 uses `rectangularBody(cols,rows)` — must take the body ring in outline mode; V13 stays auto-only by decision. Implementation scope = the doc's §6 table (Task #11's new blocker description).

**2026-08-02 (later) — LG-P1's Seam-Coupon.bkr AUTHORED (bikar main `73514f1`, pushed); the two Seam-Coupon baseline entries below are GONE (3d-models `b48eae2`, pushed).** Two load-bearing findings: (1) the catalog's "one straight relief line" was self-defeating — mural recentres art by segment bbox onto the panel mid-line, which is a 2×2 piece's only anchor candidate, and `solveAnchors` drops anchors within reach (~tube⌀/2 + rib + 0.4 ≈ 3.8 mm) of a pocket ring → zero clutch (V-M6) on the very coupon that measures clutch registration; the authored coupon carries **two bars at ±5.5 mm** and the catalog/backlog entries record why. (2) **LG-B2's Rosette-Brick.bkr CANNOT be authored against today's grammar**: the pattern-outline body is specified (lego-lab-design §5.2/§7.2) and the kernel honours `BrickSpec.bodyOutline` (tests only), but no BrickStmt reaches it — `evaluateBrickDecl` builds every DSL brick rectangular, and an auto rule would silently rebuild Star-Brick. Needs an explicit bikar DSL extension (corpus-sweep name gate, parser, evaluator via unionPatternFaces+insetRing, decision doc) — **pending user green-light** (Task #11; recorded in backlog note 10, 3d-models `adeff35`). Construction circles in a pattern render in SVG but contribute nothing to mural ink/relief — only bounded faces of drawn edges do.

**2026-08-02 — Mural feature MERGED AND DEPLOYED: bikar #64 squashed to main `dc0de58`, 3d-models #18 merged to master `ed074c0` (merge commit, deliberately — the map's as_of pins its own branch commit, so squash would orphan it), gallery deployed to gh-pages `de88ce6` and verified live (Star Mural entry + web image + composite and 4 per-piece STLs at blog.bytesofpurpose.com/3d-models). The mural use case is UC20 — master's #16 claimed UC19 for the LDraw read-back panel while the mural branch was in flight; read-back shipped first and kept the number.** Post-merge chores done: bikar's grammar.md grandfather entry deleted (bikar CI *forced* it pre-merge — the pointer ratchet flags entries that resolve, and the target resolved the moment the 3d-models branch was pushed), use-case map repinned to `e0af54b`/`dc0de58` with 12 merge-shifted anchors corrected. A merge commit whose map is conflicted can't pass the staged-map hook (as_of must equal a commit that doesn't exist yet): resolve with HEAD's map + USE_CASES_OK=1, then land the merged map next commit with `--refresh`. **Supabase sync FIXED 2026-08-02**: repo secrets SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY set on NaqshCoffee/bikar by piping `dotenvx get <NAME> -f .env` (bikar root `.env`, plaintext; URL comes from VITE_SUPABASE_URL) into `gh secret set` — values never displayed. Second failure then surfaced: supabase-js realtime needs native WebSocket (Node ≥22) and sync-patterns.yml pinned Node 20 → bumped to 22 + added workflow_dispatch (bikar main `63d5cf3`); dispatched run 30749674032 green — 104 patterns upserted to bikar_canonical_patterns, 6 seeded to ch_patterns. Note: a rerun of a failed run reuses the workflow definition at the original SHA, so workflow fixes need a fresh run (hence workflow_dispatch). bikar primary checkout still left on the merged (now remote-deleted) feature branch because `main` lives in the bikar-lego-lab worktree. Later on 2026-08-02 (bikar `8f22ff3`): coffee-house-sites' dotenvx sidecar machinery ported to bikar (.env.encrypted committed, env-* Makefile targets, pre-commit plaintext/keyfile gate, 34 tests, `.claude/skills/manage-secrets/SKILL.md` inventory skill); setup-secrets.sh now pushes the Supabase pair to GitHub Actions (the original gap) via stdin pipes. **DONE 2026-08-02 — the dotenvx key IS backed up.** `make env-keys-push` from the bikar-lego-lab worktree created LastPass entry `dotenvx/bikar`; `make env-status` then read it back through a fresh `lpass sync` and reported in-sync (fingerprint `d787f6a7e2c6`, identical in both local checkouts, 424 B, mode 600) with `.env == .env.encrypted` over 10 keys. Two details worth keeping: `env-keys.sh` derives the entry name from the **origin remote**, not the directory, so pushing from a worktree cannot mint a second entry; and `status` syncs before comparing because a push followed immediately by a cache-read once falsely reported "NOT backed up" — which invites a redundant push, the one operation that overwrites the vault copy. Load-bearing facts: (1) **a mural's fused STL fails `--check` by design** — the single-mesh path holds the panel to the 1.2 mm orb floor while each piece is entitled to the 0.7 mm brick floor (Star-Mural measured 0.7568 mm); `make pattern-sets` gates per piece via `--format parts --check` and writes the composite ungated for preview, and `make bricks` skips `mural` presets. (2) Seam continuity is by construction (cut lines injected into one planar-graph extraction ⇒ shared vertices), never tolerance; art cut at nominal 8 mm lines, the 0.2 mm gap stays physical (D-013; reversal only via LG-P1 measuring a jog > 0.2 mm). (3) The use-case map's `--refresh` deliberately pins siblings to **origin/HEAD**, so a map row pointing at unmerged bikar work needs a hand-pin to the branch commit (pushed first so it resolves in a fresh clone) — currently bikar pinned at 1e008fc; the first refresh after bikar#64 merges folds it back, and same-commit 3d-models pointers force a two-commit flow (surfaces first with `USE_CASES_OK=1`, map second). Post-merge chores: delete bikar `.doc-pointer-baseline.json`'s grammar.md→lego-pattern-set-design.md grandfather entry; the two Seam-Coupon.bkr entries in 3d-models' baseline go when LG-P1's coupon is authored. Print coupons LG-P1/LG-P2 registered but HELD pending printer.

**2026-08-01 — `bikar-studio.pages.dev` is PUBLIC and publishes this PRIVATE repo's 102 pattern sources; that is now declared and gated, but not changed (bikar PR #58, `a3c2472`).** The `?raw` eager glob in `starter-patterns.ts` inlines every `.bkr` into the bundle, so the DSL source ships to anyone who loads `/editor` there. `packages/web/public-surface.json` declares each host's exposure; `public-surface.test.ts` (`npm run check:surface` + a husky block) pins the published count to `patterns/index.json` so growth can't land silently, and pins every `import.meta.env.VITE_*` read; `check-deploy.sh` reads the declaration and **inverts** its assertions for an `access` host; `scan-bundle-secrets.py` reads the built bundle before upload (gitleaks reads source diffs and cannot see build-time inlining). Third surface nobody had named: the permanent `<hash>.bikar-studio.pages.dev` per-deployment alias — public, unbounded, declared as a wildcard. **Pending Omar: whether pages.dev should be public at all** — adding it to the Access app's `self_hosted_domains` is a Cloudflare dashboard change, after which flipping `exposure` to `access` makes the gate enforce it. Deliberately NOT gated: whether Access is actually on — `cf-setup.sh:76-78` skips its whole Access step without a Zero Trust token, so a repo-side check there would go green while the site was open.

**2026-07-31 — ALL FOUR REPOS CLEAN AND PUSHED. This supersedes every "NOT PUSHED" note below, including W2 and Lego Lab R0.** bikar `main` 5c13824, qiyas `main` 9a3b78c, sacred-patterns `master` 5f7e058, 3d-models `master` in sync with origin. The Phase C calibrate machinery is live and merged: `Calibrated<T>` + `CAL_BETS` in `bikar/packages/core/src/kernel3d/calibration.ts`, the shrink-only `.calibration-baseline.json` gate, `patterns/Coupons/Machine-Card.bkr`, `3d-models/docs/calibration-design.md`, and the `calibrate` skill. 14 records, all `provisional` — **printing is still on hold, so no CAL bet is settled and no constant is earned.**

**2026-08-01 — the 42P10 class now has a guard, and it lives in coffee-house-sites, not bikar.** `make supabase-check-upsert-keys` (chs PR #8, `0441b41`) scans every `on_conflict=` target in **both** repos — chs `functions/ workers/ scripts/` plus `bikar/packages/web/functions/` — and checks each against the live unique indexes, rejecting partial and expression indexes because `ON CONFLICT` inference cannot use them. It errors rather than narrowing when it can't find a bikar checkout, which is also why it is not a hook: it needs the network and both repos, and a check that sees half the system must not be the thing that says "clean". Companion `query.sh --status` distinguishes a paused project from a bad query ([[supabase-paused-project-timeout]]). bikar PR #43 adds **Tenet 31 — "an acknowledgement is not an outcome"** (five witnesses: the two `[0]`-on-a-PostgREST-error handlers, the zero-row table, the 12-table-short ERD at exit 0, the 34 green-local/red-deploy runs) and puts `coffee-house-sites` in `check-doc-pointers.ts`'s closed `SIBLING_REPOS` set — note both `coffee-house-*` repos live under `git-naqshcoffee/`, a different parent, so their pointers are *skipped, never resolved*: a typo in one is caught by nobody.

**2026-08-01 — the folder-move UI exists (bikar PR #49, `c9cf3fb`), which closes the "cannot be performed" note below.** Drag a file onto a folder header to file it; drag it onto the root zone that appears under the tree to unfile it. Two rules worth carrying: **(1) root is an ABSENT `folder` key, never `''`** — `'' ?? null` is `''`, so an empty string survives the handler's coalesce and lands in the column as a folder named `""` that no header renders, while the tree draws the file at the root because `''` is falsy; row and display disagree and nothing says so. `setPatternFolder` in `packages/web/src/pattern-api.ts` folds `''` into the root case, and lives there rather than in `main.ts` because `main.ts` needs a DOM and cannot be imported by vitest — a rule that cannot be imported cannot be tested. **(2) dev's folder store is `patterns/.folders.json`, a three-state overlay, and the last two states are not interchangeable** — `name` absent = ask the disk, `name → "Orbs"` = filed there, `name → null` = deliberately at root. Recording a root pattern by *omitting* its entry hands the question back to the disk, which still says `Orbs/`, so the move undoes itself on reload. The overlay reader throws on an unparseable file rather than returning `{}` (Tenet 29): the disk seed is a real, different, plausible-looking hierarchy, so failing open means the user watches folders revert with nothing to suggest a broken file. A folder is a label, not an object, so dragging the last file out dissolves it — `knownFolders` keeps it droppable for the session so the move stays undoable. Also: `packages/core/dist/` is gitignored, so a stale local build makes `npm run typecheck` fail in `packages/cli` with phantom missing-property errors — `npm run build` first (same trap recorded at M1). **Deploy verified at the bundle, not at the green run** (Tenet 31): `deploy.yml` publishes `packages/web/dist` to Cloudflare Pages project **`bikar-studio`** → `https://bikar-studio.pages.dev`, custom domain `https://bikar.naqshcoffee.com` (302 → CF Access). The `bikar-studio-aur.pages.dev` alias recorded below is from the 2026-07-27 manual `npx wrangler` deploy and is **not** what the workflow targets. Two traps when grepping a deployed bundle: the studio is **`editor.html` → `assets/editor-*.js` + `editor-*.css`**, while `assets/main-*.js` is a 0.07 kB shim for the landing page — grepping it finds nothing and looks exactly like a stale deploy; and minification mangles function names, so grep for a **string literal or CSS class** (`file-list-root-drop`, `drag-active`), never an identifier like `setPatternFolder`. Content hashes matching a local `npm run build --workspace packages/web` is the check that actually settles it.

**2026-07-31 — the studio's `folder` round-trip is LIVE; the production DB migration is DONE and is no longer Omar's to run.** coffee-house-sites migration `20260730_01_bikar_patterns_folder_and_upsert_key.sql` applied to Supabase `gmwmrcmfywsdescglijg`: `bikar_patterns.folder text NULL` (NULL *is* the tree root) + `bikar_patterns_user_email_name_uidx` UNIQUE on `(user_email, name)`. The table held **0 rows** at apply time — that emptiness is the evidence that every studio save had been failing `42P10` since the `?on_conflict=user_email,name` upsert was written, because PostgREST rejects `ON CONFLICT` without a matching unique index. Verified against the DB, not against the 201: a real upsert inside a `do $` block raised our `P0001` sentinel instead of `42P10`, then rolled back. coffee-house-sites PR #7 (`7848490`, ERD regenerated); bikar PR #41 (`f98b38c`) makes the handler send `folder: body.folder ?? null` — **`?? null`, never an omitted key**, because `Prefer: resolution=merge-duplicates` overwrites only the columns the body carries, so an omitted `folder` cannot move a pattern back to the root. Ordering mattered and was deliberate: column first, client second — sending an unknown column 400s the whole save. Cloudflare deploy watched green on `f98b38c`. **2026-08-01 correction — the planned browser check (save into a folder → reload → drag to root) cannot be performed, because the studio has no folder-move UI at all.** `packages/web/src` has zero assignments to `.folder`, zero drag handlers, and `BkrFile.folder?: string` cannot even hold `null`; every `folder:` in `main.ts` is a hardcoded `STARTER_FILES` literal. A root save therefore *omits* the key (`JSON.stringify` drops `undefined`) and only the handler's `?? null` turns that into a clearing write. The server/DB half IS verified against production (rolled-back `do $` block, 2026-08-01): into-folder sets it, an omitted key preserves it — the merge-duplicates trap — and an explicit NULL clears it. What is unbuilt is the UI that would let a user file or move a pattern. See [[supabase-paused-project-timeout]] — the project was paused when this started.

**The studio deploy was silently dead for four days (2026-07-27 → 07-31, 34 consecutive failures)** while `npm run build` stayed green, because `deploy.yml` hand-listed two workspaces and `packages/web` imported `@naqshcoffee/bikar-knobs` without declaring it — npm hoisting makes an undeclared sibling indistinguishable from a declared one until something builds a *subset* of the graph. Fixed in bikar#32 (`--workspaces` build + `scripts/workspace-deps.test.mjs`, an AST-based manifest gate). One deploy then published **81 commits at once**. Lesson: a green local build proves nothing about a deploy that names its dependencies by hand — check `gh run list --workflow=deploy.yml` after merging, don't infer. See [[gh-auto-merge-footgun]] for the related merge-timing trap.

**2026-07-27 — EVERYTHING PUSHED + DEPLOYED (supersedes the per-milestone "NOT pushed" notes below):** all orb work PR'd and merged on user authorization ("do pr and merge... then deploy"): 3d-models PR #1 (merge ad188c6, public repo), bikar PR #3 (merge 7b86ee3, private), qiyas PR #1 (merge 0c75f57, private). Pre-push leak audit: gitleaks clean on all outgoing ranges; no build artifacts/canonical fixtures/credentials/home paths in diffs. Gallery deployed via `make deploy` (gh-pages, live at https://blog.bytesofpurpose.com/3d-models/ — index "27 designs", lab.html + STLs verified 200); gh-pages `.DS_Store` scrubbed + Makefile deploy recipe now deletes them pre-commit (master 76c44c8, pushed). Bikar studio: Cloudflare Pages project **bikar-studio** CREATED first-time on the personal Cloudflare account and deployed → https://bikar-studio-aur.pages.dev (wrangler must run via `npx` — bare `wrangler` not on npm-script PATH); its /api/* functions need secrets the user must set via packages/web/scripts/setup-secrets.sh (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, CF_ACCESS_TEAM_DOMAIN, CF_ACCESS_AUDIENCE — never handle these). Still owner-gated: sacred-patterns contract amendment + qiyas-schema npm publish/tag. P2 validation plan+design delegated to a Plan subagent (first subagent use in this project — earlier design/validation docs were all authored inline).

Omar is building 3D-printable **Islamic geometric orbs** (star-lattice spheres, STL) across three repos:
- **bikar** (`~/Workspace/git/bikar`) — the `.bkr` compass-and-straightedge DSL (TypeScript, npm monorepo, `@naqshcoffee/*`). All new 3D work lives here (decision: bikar-native engine, not Python/OpenSCAD).
- **qiyas** (`~/Workspace/git/qiyas`) — Python CV validator; will gain 3D validation via per-symmetry-axis orthographic renders (M3).
- **3d-models** (this repo) — publishing end: Makefile `orbs` target + gallery (M4).

Approved plan (2026-07-23): `~/Workspace/git/3d-models/.claude/plans/sprightly-jingling-milner.md` — milestones M0–M4, both families in v1 (Family 2 pierced lattice + Family 1 woven strapwork; bikar's `kernel/strapwork.ts` already solves 2D over/under). Research handoff: `~/Downloads/islamic-geometric-orb-handoff.md`.

**M0 done + verified**: `bikar/packages/core/scratch/orb-spike.ts` (run: `npx tsx packages/core/scratch/orb-spike.ts`) — icosahedron, per-face triangle+hexagram pattern via `compileToGeometry`, barycentric lift, welded, sphere-projected, per-void inset-ring solidify (**no CSG/earcut needed** — band = union of void-minus-inset quad-strip rings), binary STL. Gate: 0 bad edges, genus 199 (=20×10 holes −1), 45.7cm³, OpenSCAD/CGAL "Simple: yes".

Key conventions: pattern authored on regular n-gon circumradius 100, corners at 90°+k·360/n CCW; winding rules for top/bottom/wall quads are in the spike — the hole wall must run `(vi_i_out, vi_j_out, vi_j_inn, vi_i_inn)` (first attempt was flipped and produced 1680 bad edges). Strut inset limit: star-tip voids degenerate if inset > ~5 pattern units (3mm struts at R=60 ⇒ 4.12u, OK).

**M1 done + verified + committed (bikar `77ac967`, 2026-07-23; NOT pushed)**: `orb` declaration end-to-end — parser/evaluator/`kernel3d/` (`polyhedra`, `face-frame`, `weld`, `solidify-lattice`), `math/vec3.ts`, `render/mesh-emitter.ts` (`emitBinarySTL`), CLI `--format stl`, examples `patterns/Orbs/{Star-Orb,Dodeca-Orb}.bkr`, docs (language-reference orb section, architecture Stage 8, decision `2026-07-23-orb-kernel3d-ring-solidify-no-csg.md`). Verified: 50/50 kernel3d tests; tsc core+cli (needed `npm run build --workspace packages/core` first — CLI typechecks against core's `dist/`, stale dist ⇒ phantom missing-export errors); eslint clean; both example STLs = CGAL "Simple: yes"; Star-Orb CLI output byte-equivalent stats to the M0 spike (5040 tri, 45.7cm³); madge clean; 2D SVG path regression-checked.

Known pre-existing bikar issues (NOT ours; verified identical at clean HEAD 86daeb6): (1) ~3 strapwork/girih tests flake with 5000ms vitest timeouts under full-suite CPU load but pass in isolation — `starter-compile` Moroccan-12-Tiled, `evaluator` "girih field + strapwork", `girih-tiles` "decoration-only weave"; (2) `npm run format:check` fails on 92 files repo-wide, so `npm run ci` is red at HEAD — don't "fix" by reformatting tracked files (huge unrelated diff); new files were formatted.

**M2 done + verified + committed (bikar `0fba3b3`, 2026-07-25; NOT pushed)**: Family-1 woven orbs — `weave crossing alternating amplitude <mm>` orb statement; `kernel3d/weave.ts` (lift+weld edge network onto sphere, tangent-plane crossing pairing, strand trace through degree-2/4 nodes, parity 2-coloring over strands with hard errors on odd cycles, per-strand closed rectangular tubes at ±amplitude — no inter-tube welding, aggregate euler must be 0). `patterns/Orbs/Weave-Orb.bkr` (hexagram WITHOUT `edges from tri` — face-boundary edges make unweavable degree-6 nodes): 26 strands (6 icosidodecahedral great circles + 20 face triangles), 150 crossings, 1440 verts/2880 tris, 30.1 cm³, CGAL "Simple: yes". Clearance: gap = 2·amplitude − depth (1.6/2.4 → 0.8mm linked chainmail); amplitude < depth/2 fuses but breaks CGAL simplicity. Decision doc 2026-07-25-orb-weave-strand-tubes-parity-2coloring (tag `orb-weave`).

**M3 bikar half committed (`7b87182`, 2026-07-25; NOT pushed)**: `--format views` per-symmetry-axis front-hemisphere SVGs + per-view gt.json (GT_SCHEMA_VERSION 1.24, `orb_view` envelope, `data-orb-view`/`data-projection`/`data-orb-base-face` attrs), mesh gate on `--format stl --check`.

**M3 qiyas half done + committed (qiyas `a4f39aa`, 2026-07-25; NOT pushed)**: SCHEMA 1.22 (Contour orb fields + OrbValidateWarning + OrbViewResult/OrbValidateReport), svg_primitives carry-through, `qiyas orb-validate` (src/qiyas/orb_validate.py) — scores encoding vs RAW bikar gt **type-agnostically** (scipy Hungarian on unit-square centers, 0.02-diag acceptance; typed compute_diff self-diff caps at 0.67 from regular_polygon-vs-unknown vocabulary split, measured). 15 tests + fixture pair tests/fixtures/orb-views/WeaveOrb.face-3.*; contract rows PROPOSED (owner-gated in canonical sacred-patterns; mirrors only); Makefile ORB_ATTR_EXEMPTIONS; contract schemas regen'd. Full suite 2738 green after companion fix `a6cf5e2`: frozen face_class audit was red at HEAD since 2026-06-11 (22e07ba added square-and-diagonal-line corpus fixture w/o frozen entry; ci-local-fast skips that audit) — recorded (3,1). Canonical fixture orb-star-vertex5.{png,json} is local-only (fixtures-canonicals/ gitignored by design). Schema mirror vendored to bikar `7b6ba29` (byte-identical diff+encoding, codegen, typecheck green; NO tag/publish — owner-gated). Pre-existing qiyas gate debt (NOT M3's): semgrep/bandit cli.py:1699 sha1, vulture _skimage_adapter, codespell review/cli.py.

**Studio orb mode (bikar `f5b89ae` + `3ef3e56`, 2026-07-25; NOT pushed)**: bikar studio (packages/web) orb mode defaults to an interactive **3D preview** of `result.orbMesh` (canvas 2D painter's algorithm, flat-shaded gold + darker bronze inner walls, NO backface culling so piercings reveal the interior, drag-rotate/wheel-zoom, module-level yaw/pitch/zoom persist across files) with per-symmetry-axis validation views as tabs beside "3D" (projectOrbView/renderOrbViewSVG scale 4; axis fallback hero = highest fold, vertex>face>edge); orb mode bypasses 2D inspect tooling; Star/Weave/Dodeca-Orb baked into STARTER_FILES folder "Orbs" (dev also lists repo patterns/ via /api/patterns). `3ef3e56` also fixed a pre-existing dev-only bug: loadPatternsFromAPI reset activeIndex to 0 before switchToFile(0), whose save-current-content step wrote the old editor buffer into the new array's index 0 (Hexagram showing Star-Orb source in memory); now keeps the hash-selected file and syncs the buffer before switching. Verified in vite dev + Chrome (devtools MCP — claude-in-chrome extension was not connected): all 3 orbs 3D render (Star 5040 tris/45.7cm³, Weave 2880/30.1, Dodeca 2520/37.9, all watertight), drag rotation works, axis↔3D tab switching works, Hexagram 2D mode + clean buffer confirmed, zero console errors.

**Rosette orb family done + committed (bikar `f9eab09`, 3d-models rosette commit, 2026-07-26; NOT pushed)**: three new orbs from a reusable rosette recipe — kite petals under `rotate N` to face corners + exact edge midpoints (edge chord x center ray construction-line intersection, point name = later-declared line first, e.g. `R0_E4.cpt0`), chained through a shoulder ring (r60, 2N points) with inner ring r38 so petal inner edges zigzag into a pierced 2N-star core. Rosette-Orb (dodeca, 10-petal, genus 251, 6480 tris, 47.0 cm³, qiyas 0.954), Rosette-Cube-Orb (cube, 8-petal, genus 101, 30.7 cm³, qiyas 0.975), Rosette-Weave-Orb (midpoint-only petals with shoulders under corners → all even-degree nodes, weave amplitude 1.6 → 10 interlaced closed ribbons, 27.9 cm³, qiyas 1.000). All mesh-gate PASS; studio STARTER_FILES 'Orbs' + 3d-models gallery (§ 02 now 6 plates, ORBS array in index.html is hardcoded — extend it for new orbs; header "22 designs"; ASSET_VER bumped) updated; all three render in studio 3D (verified in dev). Icosahedral rosette rejected (corner/midpoint radius ratio too extreme); `hankin` alone yields stars, not rosettes; `connect every 2 on C1` decagram layer cuts sliver voids below strut-inset limit — omit.

**Orb Lab design review v2 (3d-models `b43a24b`, 2026-07-26; NOT pushed)** — ALL open questions resolved with Omar via AskUserQuestion: (1) separate `lab.html` (not embedded); (2) radius ceiling from a **print-target picker** — machine dropdown (Bambu X1C/P1S/A1/mini, Prusa, Ender + SLS/MJF service presets) + custom X/Y/Z, `2R ≤ min(XYZ)−10`, lives in localStorage NEVER share URLs, implies process (FDM ⇒ weave warning); (3) **knobs = DSL-native `param` declarations** (Omar's own proposal, supersedes detach/marker options): new bikar milestone **M5** — `param name = v [range a..b [step s]] [advanced]` first-class syntax, `$name` in orb-level numbers, param-override entry point, refactor committed orbs into self-describing templates (golden-STL byte-identical at defaults), bidirectional slider↔code sync, custom orbs declare their own knobs, URL keys = param names + `f=<script-id>` (`f=custom&code=<lz>`); engine plumbing half-exists (NumericExpr `var` + const-eval binds, parser.ts:178-195, only for-loops bind today); cross-param clamps (inner<shoulder−8, amp≥(depth+0.4)/2) stay UI-side; (4) knob layer folds into **@naqshcoffee/ui** (not a new package; keep tree-shakeable); (5) **advanced disclosure from day one** (accepted more tier-2 errors, error panel = safety net); (6) add `.claude/skills/bikar-dsl` authoring skill + pre-commit/npm mesh-gate check on changed .bkr.

**Orb Lab design doc done + committed (3d-models `208e013`, 2026-07-26; NOT pushed)**: `docs/orb-lab-design.md` — pre-implementation design for a knob-driven orb configurator in the catalog + bikar-studio Dials-vs-Code tab (user requirement). Key decisions: archetype templates (code-gen `.bkr`, per-base constants from committed reference orbs — never raw grammar, cross-face closure is why), archetype×base matrix with 🔬 calibration-gated cells; three-tier validity (UI clamps for silent-invalids: inner<shoulder−8, weave amplitude≥(depth+0.4)/2, depth≥1.2 / engine hard errors surfaced verbatim / meshGate readout gating STL download); URL sync short keys `v=1&t=&b=&r=…` (replaceState debounced 200ms for drags, pushState for archetype/base, default elision, `t=custom&code=<lz-string>` ≤1800 chars else .bkr download); architecture = new bikar `packages/orb-templates` + `packages/lab` (vite, worker evaluate, Canvas-2D painter port) vendored into 3d-models via `make lab` (mirrors `make orbs`); P0=rosette lab core, P1=star/weave+calibration, P2=custom+studio dials tab, P3=polish. Verified engine facts: orb grammar = base/radius/inscribe/project/struts/pierce/weave ONLY; NO per-face inscribe filter; subdivide=icosa-only 1..4; struts always rectangular; weave only `crossing alternating`; emitBinarySTL+meshGate ARE public core exports (CLI-only consumers today); core bundles static, no three.js. Research grounding: Najm rosette=(θ,h,φ) ancestor of inner/shoulder knobs; Bonner family angles as θ detents (180°/n multiples); no browser Islamic-sphere configurator exists (gap).

**M5 done + committed (bikar `d1a0d43`, 2026-07-26; NOT pushed)**: DSL `param` declarations — `param <name> = <default> [range <min>..<max> [step <s>]] [advanced]`, parse-time substitution, `ParamSpec` on `EvaluationResult.params`, `ParseOptions.params` overrides via `compileToGeometry(source, options?)` (out-of-range override = hard ParseError, never clamped by engine). Both rosette orbs refactored to param headers (radius 60/40..110/5, inner 38/16..58/2, shoulder 60/48..76/2, strut_width 3/1.5..6/0.5 adv, strut_depth 2.4/1.2..4/0.2 adv) — golden STLs byte-identical at defaults.

**Orb Lab P0 done + committed + smoke-tested (bikar `03e7153` + CSS fix `3269ab6`; 3d-models `dfae231` gallery/Makefile + `1977169` design-doc status; 2026-07-26; NOT pushed/deployed)**: `bikar/packages/lab` vite app (lab.html, worker evaluate w/ seq-stale guard + stale-while-revalidate, Canvas-2D painter viewer port, knobs w/ touched-set override model — only user-touched knobs sent as overrides/URLs, untouched refresh from result so derived defaults recompute; tier-1 UI guards inner≤shoulder−8 + amplitude≥(depth+0.4)/2; print-target machine picker w/ localStorage + radius ceiling chip; URL `v=1&f=<id>&<param>=` w/ default elision + clamp toast; meshGate panel gates STL download). Vendored into 3d-models by new `make lab` (lab.html + assets/ gitignored on master, published by `make deploy` which now depends on lab); gallery rosette plates got "Open in Lab" deep links. Verified in Chrome via chrome-devtools MCP: clean boot, URL clamp round-trip, script switch, machine ceiling, vendored-site no-404s, gate PASS. DEVIATION vs design doc §8: knob layer lives in packages/lab, NOT @naqshcoffee/ui (external private GitHub-Packages pkg needs GITHUB_PACKAGES_TOKEN, unset → npm E401; also why bikar package-lock.json was not regenerated); fold deferred to P2. Screenshot: 3d-models `build/orb-lab-p0.png` (gitignored).

**Orb Lab P1 done + committed + smoke-tested (bikar `0eab01f` + CSS fix `c49c737`; 3d-models `19086ce`; 2026-07-27; NOT pushed/deployed)**: five new archetype orbs (patterns/Orbs/: Hankin-Orb θ param 54/18..80 — R=60/w=3 passes θ 10..84, w=6 needs θ≥18, all four extreme corners PASS; Star-Cube `connect every 3 on C1` octagram; Star-Octa + Star-Tetra canonical tri hexagram; Weave-Dodeca pentagon `connect every 2` + weave → **10 interlocked ribbons**, euler 0); qiyas composites Hankin/Star-Cube/Star-Tetra/Weave-Dodeca 1.000, Star-Octa 0.992 (warn-only vertex-4 shape-count 17v16 — encoder quirk at 4-fold tangencies, clears gate). Lab: axis-view tabs (worker renders symmetry views lazily on demand, seq-stale drop separate from evaluate seq; **verified byte-identical path data to CLI `--format views`**, only scale 4 differs), weave gate rows (`ribbons: N interlocked`, `ribbon gap: 2·amp−depth ✓/✗ fused` at 0.4mm), FDM-process warning for weave family tied to machine picker, 11 preset chips, all STARTER_FILES synced to studio. meshGate presentation note: Lab prints measured/declared, CLI prints declared/floor — same data. CSS traps fixed in c49c737: SVG width/height presentation attrs lose to CSS `width:auto` (viewBox SVG fills container — use definite square card + svg 100%); sticky viewport-height stage so 11-chip panel scroll doesn't take the preview off-screen. Gallery: 11 orb cards all with Lab deep links, "27 designs". Screenshots build/lab-p1-{hankin-axis-view,weave-dodeca}.png (gitignored).

**Orb Lab P2 done + pushed + deployed (2026-07-27)**: six bikar main commits — `52e7990` P2.1 knobs extracted to `packages/knobs` workspace pkg (durable home; @naqshcoffee/ui fold abandoned — token requirement), `59c1b8a` P2.2 worker watchdog/generation-respawn/64KiB size guard/SVG escaping, `bade84e` P2.3 custom-orb code drawer + verbatim errors + stale-while-revalidate + bake, `5e51850` P2.4 share links (lz-string `code=`, 1800-char budget → .bkr-download degrade, localStorage draft, damaged-payload toast), `228466b` P2.5 studio Dials mode + Lab→studio `#code/` handoff + "Open in Studio", `ba5ef85` P2.6 §4.3 trust badge (validated ✓ score at defaults / calibrated range / custom-not-validated) + 10 Playwright specs (6 Lab, 4 studio; Lab preview on :4613 — default :4173 collided with an unrelated user vite preview and `reuseExistingServer` silently tested the wrong site) + L6 constraint units + authoring notes. All 11 preset composites re-swept in Docker and recorded in scripts.ts (Rosette .954, Rosette-Cube .975, Weave .997, Star-Octa .992, rest 1.000; five P1 values reproduced exactly). **qiyas orb-validate needs the `:dev` Docker image (`cd qiyas && make build`) — ghcr latest/v0.1.2 predate M3 and lack the command; host venv lacks libcairo.** 3d-models `e0e0c88` pushed (lab-smoke vendoring check inside `make lab` — `$(MAKE) -C ${ROOT_DIR}` because the recipe cd's into bikar; §10 P2 status entry); gh-pages `a9d21e3` deployed with the trust-badge Lab. **Pending user: redeploy bikar studio to Cloudflare (user-run, secrets) — prod studio predates P2.5, so "Open in Studio"/`#code/`/Dials don't work there yet.** Watchdog-bomb e2e lesson: `subdivide` bombs error fast on the degenerate-void guard; a `divide C1 into 1200`+`connect every 541` star polygon gives ~9s honest compute.

**Composition design phase (2026-07-27, 3d-models pushed)**: three pre-implementation design docs in 3d-models/docs/, all DRAFT v1, no code yet — `print-validation-design.md` (`792c593`: slice-sim gate F1–F7, --check print, rides 2D-boolean dep), `piece-composition-design.md` (`6782aa9`: piece/hole-z-bands/contract-bearing port/one connect verb/assembly, orb `open cap` ring stitching → orb lamp; C1–C4 ladder; Manifold = lazy opt-in backend only for 4 forcing ops), `tile-wall-design.md` (`bb41126`: tile/wall/connector library/crop drop|clip|stretch, craft validators — sliver rule, gap formula 3×tol+CTE, face-registering zero-strain PETG corner clips, compliant field joints, anchor pull-out budgets; W1–W3 rides C1/C2). Grounded in two parallel research subagents (code-CAD prior art: borrow BOSL2 anchor triple + build123d named joints + OnShape hole-mints-connector, avoid solvers/selector-naming/attach-zoo; tile-craft field survey: fit ladder press −0.10/snug +0.05/sliding +0.15/free +0.35 diametral + printer-profile hole compensation, PLA creep bans sustained-deflection clips, PLA CTE ~10× ceramic → compliant interior joints). Also `make site` (:8613 HTTP serve — Lab worker can't load over file://) + `make experiences` map (`1fc474f`), bikar `make lab-dev` :4613 (`9dd5a98`). Both research reports checked in verbatim at `docs/research/{code-cad-composition-survey,tile-craft-field-survey}.md` and all three docs' Appendix A rewritten as linked source lists (`f59359b`) — full URL provenance now lives in the repo, not just the conversation. **Adversarial grounding audit applied (`7fdb7e1`, pushed): all three docs now DRAFT v2** with per-doc Appendix B (counter-evidence + divergence justifications), three verbatim audit reports at `docs/research/*-grounding-audit.md`, and Errata sections on both survey files. Headline reversals: tile connectors now opt-in (`connect none` default — Art3d glue-up precedent won), clip renamed low-preload (Dzus/bayonet prior art), PLA rule re-based on embrittlement-on-flex, print-gate overhang default = slicer auto rule (half extrusion width) with F5 split error/warn (Arachne floor 0.1mm), piece novelty claim narrowed to contract-on-port+connect-time-failure (BOSL2 screws.scad already separates tolerance class vs $slop), no-CSG rationale rewritten (Manifold is OpenSCAD's default since Aug 2025 — our reasons are the 2D layer + semantic labels), connect graph must be a tree (loop closes become checks), radial grub hole deleted (z-band model can't express it pre-C4). Methodology encoded as reusable skill `.claude/skills/ground-design-doc/SKILL.md` (`a9e8487`). Tasks #20–23 completed; #19 = user studio redeploy.

**Print-prep + wall-production plan (2026-07-27, 3d-models `a261600`, pushed)**: catalog P2 print plan delivered to Omar (StarOrb.stl at defaults verified current by byte math 84+50×5040=252,084; stock slicer settings deliberately — supports/brim are P2 questions 1/5). P1 coupons need NO Lab bake: `bikar render ... --param radius=40 --param strut_width=<w>` (CLI validates against declared ranges) — exact commands recorded in the catalog Model line; three STLs gate-clean in `build/stls/coupons/` (untracked, regenerable; minFeature 1.5/2/2.4 — at width 3 the 2.4 depth is the min feature; ~80 g trio). NOTE: 3d-models Makefile `orbs` globs `bikar/patterns/Orbs/*.bkr`, so coupon/one-off .bkr files must never land there (they'd enter the gallery pipeline). tile-wall-design gained §7.1 (wall's-worth-on-a-small-printer production plan: connect-none/gap-formula make bed size a non-constraint, print-hours is; levers = field size, module-to-bed, farm out identical middle, one filament batch) + W3 layout-report production metrics. Tasks: #24/#25 done. **User decisions (2026-07-27, AskUserQuestion)**: printing ON HOLD for now (#10 pending, plan intact); machine when it resumes = **Bambu A1/P1S/X1C class, 256³ bed** (catalog Print target stays TBD — actuals only); **next build = C1+W1** (tasks #26/#27, W1 blocked by C1); tile-wall §10 Q1 clip rebate = settle empirically in W2 coupon, both corner variants (doc updated, `863c4cf` pushed).

**C1 done + pushed (bikar `9c1f1a6`, 2026-07-28)**: `piece` declarations per piece-composition-design §9 — `extrude <region> depth` / `revolve <region>` / `tube inner/outer/height`, z-band `hole` stacks (extrude-only in C1), auto-minted hole ports in `piece3d` provenance, CLI gate reads piece minFeatureMm. All shaping 2D-before-extrusion (slab stacks cut at band boundaries; walls + earcut caps + disc/annulus shoulders; membrane guard uses closed-interval cross-hole z test). Key kernel piece: `unionPatternFaces` in evaluator.ts — exact directed-edge cancellation union over a pattern's bounded faces (needed because star-over-tile patterns are disjoint planar subgraphs whose faces overlap; unionShapes can't merge them); drops double-covered nested CCW patches, errors on CW holes/pinch/open-chain/disconnected. earcut vendored (`earcut-vendored.ts`, ISC) to keep core zero-runtime-dep. Deliverable `patterns/Pieces/Nail-Tile.bkr` (Pieces/ dir deliberately outside the 3d-models `orbs` glob): 100mm girih tile, countersunk nail bore, watertight genus 1, minFeature 1.75mm, 59.9cm³. 47 new tests; suite 1212 green; language-reference has a full Piece Declarations section. Gotchas: `tile` is a reserved keyword (can't name a boundary `tile`); vitest must run from bikar repo root; connect/port/assembly are C2, not C1.

**W1 done + pushed (bikar `6cf121e`, 2026-07-28; use-case map 3d-models `2c1a23d`)**: `tile` + `wall` declarations per tile-wall-design §9 — tile = piece specialization (outline square/border/inscribe/depth/hole; §8 margin rule ERROR at <6mm hole-to-edge; inscribed-art bbox must span [side−2·border−0.5, side+0.5] per axis), wall = `boundary rect W x H` + `module <Tile> gap <g>` + `layout centered [quartering]` + `crop clip|drop` (`stretch` = polite not-yet-supported ParseError). `kernel/wall-layout.ts` layoutWallGrid: **deliberate divergence from §3.2's sliver-only retry — ALWAYS scores all 4 half-pitch candidates** (slivers asc, fulls desc, fragments asc, centered wins ties) so a 2·pitch boundary quarters to all-full tiles; note a 201.2mm boundary's centered fragments are 49.4mm (100.6−51.2) — genuinely sub-half slivers, only the exact-2·pitch (202.4) case yields legal 50.0 halves. Wall result forwards the module tile's orbMesh/piece3d (STL = printable unit + stderr layout report via CLI printWallReport; report is STL-path-only) and composes translated/Liang–Barsky-clipped segments for SVG; fragments gate per distinct tile-local rect shape (cut-edge holes dropped at <6mm margin), advisory not fatal. Deliverable `patterns/Walls/Nail-Wall.bkr` (Walls/ outside the 3d-models orbs glob): 201.2mm² boundary, 4 full tiles/0 fragments, offset (50.6,50.6), module STL identical to C1 Nail-Tile (528 tri, 59.9cm³). 37 new tests (suite 1250 green + 3 pre-existing expected-fails). Refactors forced by the no-complexity-disable rule: evaluateFile → isSolidDeclaration/evaluateSolidDecl dispatch; parser isTopLevel → TOP_LEVEL_STARTERS Set (Tile/Wall are unconditional top-level heads — girih `tile base` blocks are consumed by parsePattern before the body loop, verified safe). Use-case map gained UC12 + re-aimed UC1/UC2 pointers (validate.py --refresh).

**C2 done + pushed (bikar `86b0dc9`…`588eeed`, 10 commits, 2026-07-28)**: assembly layer per 3d-models docs/c2-assembly-design.md — kernel3d `frame.ts` (rigid xforms, quadrant-snapped trig, `xformFromTo`/`matedFrame`) + `fit-profile.ts` (ladder press −0.10/snug +0.05/sliding +0.15/free +0.35 diametral ± 0.05; profiles none/pla_calibrated +0.20/petg_calibrated +0.25); `rod d/height` constructor; `port` grammar on piece+tile (`at centroid|x,y [z] [dir ±z] [spin]` + `kind pin d ⌀ fit <class> | pin_socket | ring/rim d depth | axis`; centroid-only on rod/tube, rejected on revolve; hole ports mint top-face +z with narrowest-band ⌀); `assembly` decl (place/connect/export parts, one instance per piece); deterministic source-order tree solve (root at identity, closure checks ≤1e-6 with recorded residuals, 12 validator error classes); CLI `--format parts` (piece-local `<Assembly>-<Piece>.stl`, `--check` gates all before any write) + `--fit-profile` (compensates printed bores only; authored ⌀ stays contract+provenance). Deliverables `patterns/Assemblies/Pinned-Tiles.bkr` (2 tiles + 2 pins, fully parametric $pin_d/$depth, closure exactly 0) + `patterns/Coupons/Fit-Coupon.bkr` (five-bore ladder + gauge; calibration decides fit-profile). 25 e2e tests incl. file-reading deliverable tests; suite 1353 green + 3 expected-fail. Docs: language-reference "Ports, Connect & Assemblies (3D)" section; decision `2026-07-28-c2-ports-connect-assembly.md` (tag `piece-assembly`; Q3 resolved: NO assembly-level param namespace — file params forward; revisit at C3 `place … as …`); roadmap C1/W1/C2 rows. Core at 0.1.4 (PiecePort now discriminated union). Next: W2 (task #32, 8 commits per w2-connector-design §10; plan in session scratchpad w2-plan.md).

**W2 done + PUSHED/MERGED (bikar PR #4, merge `6b38342`, 2026-07-29 — a print-readiness gate V1 `fe32830` `--check print` rode in on the same branch; bikar `main` has 0 unpushed commits)** — per 3d-models docs/w2-connector-design.md. Series numbering drifted (`9/8` exists; `6/8` landed last) because Omar sequenced **deliverables first, Lab last**; read the series by title, not number. Shipped: `kernel3d/solidify-slabs.ts` (per-slab outlines + disjoint islands under a **strict nesting interface contract** — identical/nested/disjoint rings only, partial overlap throws; `solidifyExtrudedPiece` untouched so W1 output is byte-identical); `clipseat corners rebate <mm>|proud` (variant word MANDATORY — §11 Q1 rebate-vs-proud is empirical, unresolved); generated `clip` CornerClips; `mount keyhole` + #6/#8/#10 screw table; wall `connect clips`; CLI `--piece <Name>`; Lab instanced wall preview. **Two decisions worth remembering: (1) clip placement is layout-derived, NEVER routed through C2 `connect`** — a clipped 2×2 grid is the canonical closure cycle, so the solver would flag a defect that isn't one on every wall; clipseat corners/keyholes still mint ports (`Tile.clipseat.ne`, `Tile.keyhole`) so C2 can reference them. **(2) the clip is exempt from the 1.2 mm FDM mesh floor, the tile is not** — verified `--piece ClipTile --check` PASS at minFeature 2.4mm vs `--piece StarClip --check` **FAIL at 0.6mm (expected, by design)**, both watertight euler 2; the tile's rebate is a *step, not a strut* and is deliberately excluded from minFeature in clipseat.ts. Detent resolved during impl as a **full-width transverse rib** (design §12): the small interior bump was both un-printable (0.05–0.32mm surrounding walls) and un-meshable (interior island under the plate — independent earcut calls don't agree, so the seam never pairs); only the plan shape changed, the 0.3–0.5mm band and the `param` stand. `crop stretch` was designed under W2 but **never shipped** (parser rejects it with a pointer to clip/drop) — now its own roadmap row, as are W3 fragment-corner clips. Coupons `patterns/Coupons/{Fit-Coupon,Clip-Coupon}.bkr` + catalog W-F1/W-C1 are `planned`, NOT printed (printing on hold), so Q1–Q4 stay genuinely open. Decision doc `docs/decisions/2026-07-29-w2-wall-connectors-mounts.md` (tag `wall-connectors`). Lab gained its first non-orb result family: orb-only UI is suppressed by payload shape (`strandCount: null`, `viewAxes: []`), not a flag; a wall's Lab STL is named `<Tile>-module` because the export is the module tile, not the wall; preset chips are an explicit §9 scope-out, so a wall is reached via the custom-script path (do NOT build a wallScripts registry). Known wart: mesh-gate failure text still says "widen 'struts width/depth'" — orb-era wording that misdirects on a clip.

**Lego Lab R0 done + committed but NOT PUSHED (3d-models `4c3b900` + `249cb5a` on master, 2026-07-29)**: design-only phase for a new "Lego Lab" — bikar pattern → pieces → 3D-printed **LEGO-compatible parts** (STL), plan at `.claude/plans/sleepy-yawning-bubble.md` (approved). Five decisions locked by Omar: printed parts NOT a BrickLink/stock-part mosaic; **both** gates (anchorability hard pass/fail + grid-fit score 0..1 with a scale/rotation sweep — his own framing, from "can we play with settings to make a pattern compatible?"); interface is a per-piece DSL option (tile-style/full-brick/edge-stud); true LEGO scale 8 mm pitch; full `/ground-design-doc` ceremony. Ships `docs/lego-lab-design.md` v2, `docs/research/lego-brick-system-survey.md`, `docs/research/lego-lab-grounding-audit.md`, catalog LG-F1/F2/R1/B1/B2/D1. The audit forced four real changes (clutch rib as a kernel feature per MachineBlocks' shrink-everything-then-re-add-clamp-bands architecture; `engage` 1.6→3.2; grid-fit rebuilt on lattice-basis residuals + rotation search because v1 read only vector *lengths* and scored hexagonal 1.0; snap 0.2→0.05 mm) and withdrawn four claims (zero-clearance framing, the 0.02 mm clutch band, the printer-accuracy figure, absolute novelty). Catalog edits trip the use-cases hook (pointer file) — `USE_CASES_OK=1` is correct here, it's UC8, not a new use case. Next: **M6** (`brick` declaration + `kernel3d/brick.ts`) and **M7** (anchor solver + `grid-gate.ts`), but LG-F1/F2 clutch coupons block M6's dimensions and printing is on hold.

**M4 done + committed (3d-models `8de323d`, 2026-07-25; NOT pushed/deployed)**: `make orbs` (bikar CLI → STL w/ mesh gate + views + gold hero-view preview via build/orb_previews.py + rsvg-convert; hero pick = highest fold, tie vertex>face>edge), .bkr vendored to src/Orbs/, index.html "§ 02 — The Orbs" (auto-fit grid), re-tracked build/*.py (process_images.py was collateral of f430461). All 3 orbs `qiyas orb-validate` PASS ≥0.95 (Star 1.000, Weave 0.997, Dodeca 1.000 — Dodeca never seen before). `make deploy` NOT run (pushes gh-pages; needs authorization). Physical print prototype pending.

**Breakdown-page rework — shipped and deployed 2026-08-19.** The per-orb breakdown
page now teaches construction rather than showing a rotating gray blob: five beats
(flat drawing → bare base solid → copies tiling the sphere → depth-cued turntable →
live viewer), every mechanism parameterised by what the manifest declares, zero
per-orb code. Maclado9Overlap reached parity on a ribbon turntable, and its stale
shipped cell views were resolved by deletion — the engine refuses to produce them by
design, so regeneration was impossible and their existence was the defect. The
scope call that governs future style work: **style every human surface, keep the gray
instrument byte-stable** — a full re-record is a three-repo cascade repeated on every
tweak, so `build/orb-views/` top level stays byte-identical and styled variants live
in a `display/` subdirectory qiyas never discovers. Landed as bikar #111 + #112
(`9d2336e`), 3d-models #86 + #87 (`39a7542`), gh-pages `6cc1b0f`; guarded by
`.claude/gates/timelapse_gate.py` (green on all 14 orbs). Closes tasks #14, #17, #35.
See [[3d-models-deploy]] for why the site 404s for ~40 s after `make deploy` returns.

**Follow-up the same day, D-037 (bikar #113 `57ee49d`):** first live reading of that
page found two defects no gate could see. The base solid was written once as frame
zero and never again, so the pattern replayed in mid-air — it is now a persistent
stroke-only **scaffold** under every stage frame, with the sphere's limb, and only
`complete` drops both (it is byte-pinned to the shipped view). And the spin's ragged
edge was `DEFAULT_FRONT_CAP_MIN_DOT = 0.3` — a *detector* constraint applied to a
picture no detector reads, capping content at `r·√(1−0.3²) = 0.954r` and making the
boundary change shape every frame; display frames now use `cull: 'back-face'` with
painter ordering by `meanDot`. **The transferable lesson: an absence rule is not a
coverage rule.** The gate's T6 asserted stage frames carry *no* style markers, and it
passed for the whole period the base solid was missing from every frame — a rule about
marks that should be absent cannot see a missing picture. T6 now fails in both
directions, and the amendment produced 632 findings on the pre-fix tree, 0 after.
