---
name: omar-working-preferences
description: How Omar decides and what he confirmed — decisions via AskUserQuestion with real options, deliverables before polish, faithful-and-risky over safe substitutes, delete merged branches, memory holds facts not to-dos
metadata:
  type: user
---

- Decides through AskUserQuestion with concrete options and has repeatedly chosen the **faithful, higher-risk** path over a safe substitute (Family 3 over a star-ball; overlap spike first; patch-first probe) and the **staged** path when symmetry makes it honest ("symmetric first", M4b as its own milestone).
- Sequences **deliverables first, Lab/polish last** (W2's series numbering drifted for that reason — read by title).
- Wants proposals grounded before code: research checked in verbatim, an adversarial audit before a design merges, and an honest "documented partial" over a distorted result.
- Confirmed process rules: every change through a PR, including docs ("Leave it, PR next time" after one direct-to-master commit); verified-merged branches get deleted ("Delete all 38"); worktree per task; durable text cites stable ids, never task numbers; memory records facts and tenets while to-dos live in the task system and `docs/plan.md`.
- Owns the calls that are taste or money: which orb to build next, whether pages.dev is public, when to print.

**Why:** knowing which way he leans turns a blocking question into a recommendation he can accept in one word.

**How to apply:** present 2–4 options with costs, recommended first; default to the robust option; hand back owner-gated items rather than working around them ([[owner-gated-and-on-hold]], [[pr-flow-for-all-repos]]).
