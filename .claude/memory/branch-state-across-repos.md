---
name: branch-state-across-repos
description: "Remote branch state after the 2026-08-30/31 cleanup, re-verified 2026-09-22 (3d-models master + gh-pages only, 0 open PRs, two detached worktrees kept, snapshot ref; bikar main only after four dead branches deleted and rods-relief #226 merged 2026-09-23; youtube has no remote and is main only after its four local branches were hand-merged 2026-09-23; 2026-09-25 third pass: bikar is a bare repo + bikar-main work tree, sacred-patterns dependabot folded into #53, qiyas/3d-model-hub main only, 0 open PRs and 0 stashes anywhere) — one kept prior-art branch, one superseded branch that must never merge, branch guards and protection since 2026-09-02"
metadata: 
  node_type: memory
  type: project
  originSessionId: b317004f-c205-413f-8ef8-7b5f99a1b742
  modified: 2026-09-25T15:01:27.621Z
---

Remote state after the cleanup (all merges sentinel-verified by content, not subject): **bikar** = `main` only; **qiyas** = `main` only; **3d-models** = `master` + `gh-pages` (deliberately diverged, never merged into master); **sacred-patterns** = `master` + `gh-pages` + `wip/react-d3-2024` (kept as cited prior art for the d3 work; `wip/weave-progress-page` was merged as PR #43 `0d3ad1e`). One bikar branch was SUPERSEDED, not merged: `fix/weave-amplitude-guard-by-depth-suffix` improved a rule main withdrew in D-042 and was deleted — if it reappears, it must not merge.

**Re-verified 2026-09-22 (second consolidation):** **3d-models** remote = `master` + `gh-pages` only, 0 open PRs. Eight squash-merged leftovers were deleted by content diff vs `origin/master`, and `feat/x2d-slice-preflight` (one commit, superseded by master's bench sheet) was deleted local + remote. The shared checkout sits on `master`; the linked worktrees `../3d-models-pm` and `../3d-models-master-ro` are clean and detached at master (kept as cheap two-way doors, not removed). The pre-cleanup shared tree is preserved at `refs/snapshots/tree-2026-09-22` (9efbc3d) if anything turns out to be missing. Only untracked file left: `.env` (dotenvx-encrypted, public repo — Omar's call whether it stays untracked or goes).

**bikar + youtube, 2026-09-22/23 (same consolidation):** **bikar** remote = `main` only. `feat/coaster-rods-relief` (PR #226) merged 2026-09-23 as 5a588e5 after Omar said merge: its five "failed" checks were billing-block phantoms (0 steps), so the proof was a hand merge of origin/main INTO the branch (four files auto-merged, every added line from both sides verified present) and a green `make local.ci-strict` on the combined tree (5240 tests) before the squash; the squash tree equals that merge tree. `../bikar-cst` worktree and the branch removed; the only bikar worktree left is `../bikar-main`, detached at origin/main with local `main` tracking it. Also merged 2026-09-23: #244 (pre-push deletion skip) and #245 (lockfile sync). Deleted by content diff against each PR's merge commit: `fix/spelling-geogebra-fixtures` (#240), `feat/coaster-colour-regions` (#213), `feat/radial-band-parts` (#221), `feat/coaster-interlock` (its 9 commits were rebased into `feat/coaster-interlock-pr` = #209; its only 2 extra lines were replaced by the diagonal-swap fix c504c11). `../bikar-main` is detached at origin/main and local `main` tracks it. **youtube** has NO remote at all and is now `main` only, one worktree (`~/Workspace/git/youtube`). Omar chose "merge all by hand, in order" on 2026-09-23 and the four local-only branches were merged as real merge commits (c22a60c fix/naqsh-coords-conjugate-recognizer, which contained all of feat/ggb-coords; e34a7aa feat/ggb-from-xml; 8d1eaeb feat/ggb-highlight) and safe-deleted with their worktrees. Every conflict was in the `.PHONY` list or inside a managed block (code map, run catalog) — see [[git-and-gh-mechanics]] for the technique. Pre-merge main is preserved at `refs/snapshots/main-pre-consolidation-2026-09-23` (283df26). 6d359b1 then fixed a pre-existing `make env-check` failure (youtube-ladder skill lacked the python-env tag). Earlier: the two `worktree-agent-*` branches (ancestors of main) and the detached duplicates `youtube-cst` / `youtube-oracles` were removed. The empty `~/Workspace/git/{3d-models,bikar}.worktrees` directories were removed. The "other active session on feat/x2d-slice-preflight" the 3d-models branch hook reports is a stale session record: that branch merged as #185 and exists nowhere.

**Third consolidation, 2026-09-25 (all six repos, every stash cleared):** 3d-models = `master` + `gh-pages`, pm and master-ro worktrees still detached at master. **bikar** is now a **bare repo** at `~/Workspace/git/bikar`; the only work tree is `../bikar-main`, detached at origin/main. Push and delete from `bikar-main`, because pre-push needs a work tree. #246 merged. **qiyas** and **3d-model-hub** = `main` only, one worktree each. **youtube** is unchanged: `main` only, no remote. **sacred-patterns** = `master` + `gh-pages` + `wip/react-d3-2024`, 0 open PRs. Its four dependabot PRs (#46/#47/#48/#51) were folded by hand into #53 (5bb7896). The one lockfile conflict kept baseline-browser-mapping 2.11.21 over 2.11.20. Every other `+` line from each PR was proven present, and the four were closed with a pointer. #53 also fixed a real gate bug: `gen-decision-ledger-xrepo.sh`, run from a hook, inherited `GIT_DIR` and read itself instead of the siblings (test case 15).

Since 2026-09-02 a pre-commit hook refuses direct commits on main/master in both repos (`BRANCH_OK=1` overrides; bikar #136 `4ac089b`, 3d-models #131 `04f2137`), and branch protection is applied: bikar main requires ci/e2e/gitleaks (admins not enforced), 3d-models master is PR-only (D-048/D-049).

**Why:** a stale local `main` can be ahead of origin with pre-rebase duplicates; "origin authoritative" was once asserted before being verified.

**How to apply:** sentinel-verify before any reset or delete; a parity test that diffs an ahead/behind count another session can move is flaky by construction — mask it. Flow rules: [[pr-flow-for-all-repos]], [[git-and-gh-mechanics]].
