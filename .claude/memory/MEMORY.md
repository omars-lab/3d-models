# Memory Index

- [autoMemoryDirectory setting](auto-memory-directory-setting.md) — relative paths and checked-in project settings are silently ignored; use an absolute `~/` path in `settings.local.json`
- [3D Models deploy](3d-models-deploy.md) — gh-pages diverged branch, OpenSCAD render + image pipeline
- [Islamic orb project](islamic-orb-project.md) — cross-repo orb build (bikar DSL 3D + qiyas + gallery); M0–M5, Orb Lab P0–P2, C1/W1/C2/W2, Lego Lab R0 and the calibrate machinery **all merged and pushed as of 2026-07-31**; mural feature **merged and deployed 2026-08-02** (bikar #64 → `dc0de58`, 3d-models #18 → `ed074c0`, gh-pages `de88ce6`; mural = UC20, read-back panel kept UC19); Supabase sync fixed 2026-08-02 (secrets set via dotenvx pipe + Node 22 bump, run green); dotenvx sidecar + manage-secrets skill ported to bikar `8f22ff3`; LG-P1 Seam-Coupon.bkr authored 2026-08-02 (bikar `73514f1`; two bars, not one — one bar deletes a 2×2's only anchor); `footprint outline` mode shipped + LG-B2 Rosette-Brick.bkr authored 2026-08-02 (bikar `bf6c602`, 3d-models `992d795`); LDraw thumbnail CLI + `validate-render` skill shipped 2026-08-07 (bikar #82 `1113052`, 3d-models #57 `3190f22` + #58 `d361785`, D-028 — .mpd→multi-angle PNGs via Playwright full Chromium, hard counts + soft goldens); maclado Family 3 M1–M5 merged + M4b closed as D-030 documented partial 2026-08-08 (bikar #85–#91 → `0fe0cea`, 3d-models #61–#64 → `aded326`; greedy chain's separations are a continuum — 34 gap tiles→33 congruence classes, tolerance-robust); M4c quantized-separation spike merged 2026-08-11 as D-031 (bikar #92 → `ec4518b` latticeWalk + polygonal gap cut, 3d-models #65 → `5df1171`; 18-wheel asymmetric walk → 13 tiles → 4 classes on 2 separations vs chain's 33/32 — quantization delivers, no orb shipped); M4d overlap spike merged 2026-08-11 as D-032 (bikar #93 → `f7bddb7` rim-crossing instrument, 3d-models #66 → `6ab47e3`; tangency touches, overlap weaves — field window ρ 1.15–1.25 puts all 30 pairs in the clean 2-crossing weave with degree-4 nodes); M4e welded woven-overlap orb BUILT + SHIPPED 2026-08-15 as D-033 (bikar #94 → `f3cb04c` maclado-woven kernel + `overlap` DSL + Maclado-9-Overlap.bkr, 3d-models #68 → `1f13a9b`, gh-pages `728d1ac`; 60 loops over 420 crossings, parity solves — the D-032 build decision is closed); **Q-series (D-034) fully closed 2026-08-16** — contract v1.5 accepted + cascaded (sacred-patterns#36 `8a9e43b`, bikar#101 `2fc4d6e`, qiyas#17 `a77ed52`), qiyas v0.3.0 tagged + published to GHCR (`dfd0f76`), Q4b merged as bikar#102 `6157792` after measurement inverted its premise (encoder floor 0.0001–0.0003 vs max_dist 0.028284; every drop a shape never produced — no constant changed); dotenvx key backed up to LastPass `dotenvx/bikar` and verified in sync 2026-08-02; **D-039 shipped + deployed 2026-08-19** — `linkageGate` measured every woven orb printing as one lump (4 of 5 outright fused); the `(strut_depth+0.4)/2` rule was about ribbon *centrelines* and a ribbon has width, so all five amplitudes re-cut from measurement (bikar#116 `546b734`, 3d-models#90 `5f3301b`, gh-pages `cc40f2b`); **D-041–D-045 shipped + deployed 2026-08-20** — five-defect queue closed (bikar#121 `8dda702`, 3d-models#98 `c40dbc7`, gh-pages `7d0ff4e`); three open calls handed back as one artifact (tasks #4 M4c orb, #36 `as_of` pin, #9 D-log → hub); **prints-tab S1 landed 2026-08-29** (3d-models #101 → `c948cf4`, D-046 — design doc + record format + zero-state survey + baseline forward-refs; the tab absorbs one event, presents the queue, deletes the catalog's 32 empty Iteration-log tables; gate `prints_gate.py` deliberately deferred to ship WITH the first real print, not over an empty set; rungs S2–S7 = tasks #66–#71 all printer-gated); **rosette→LEGO-pin explorer captured 2026-08-30** (3d-models #102 → `81fbd32`, `docs/rosette-pin-explorer-design.md` = source of record for a private Claude artifact `df5788b3-8785-492b-a5f0-92533fbad4e5` — a visual instrument for why LEGO pins land where they do; a diagnostic/roadmap doc, not a shipped feature; the working HTML lived in scratchpad, not committed); **branch survey 2026-08-30** — all 41 3d-models remote branches are squash-merged-and-left-behind or superseded (verified by sentinel content in master, not just subject match), **none hold unmerged work**; bikar's 13 candidate branches sentinel-verified 2026-08-30 (12 merged, 1 — `fix/weave-amplitude-guard-by-depth-suffix` — SUPERSEDED: it improves a rule main withdrew in D-042, must not merge) and **DELETED from origin** (8 were already gone via concurrent pruning; the 5 I deleted: assembly-place-colour, orb-validate-ci, text-solidify, thumbnail-colour-gate, weave-amplitude-guard); **branch cleanup DONE 2026-08-30** — 3d-models remote pruned to `master`+`gh-pages` only (17 branches were still live of the surveyed 41 — rest already gone via prior fetch-prunes; all 17 re-verified merged then deleted). Siblings sentinel-verified: **bikar** all 22 remaining branches squash-merged → safe; **qiyas** all 9 merged (incl. `fix/orb-view-scoreable-split` = #11, decision doc in main + 549 branch-unique lines are stale-old, not payload) → safe; **sacred-patterns** 7 merged → safe, but **2 `wip/*` hold GENUINELY UNMERGED work and must be kept**: `wip/react-d3-2024` (a 2024 d3/React experiment — `src/ts/index.tsx`+`draw.ts` absent from master, which still has the old `index.ts` it replaced) and `wip/weave-progress-page` (5 commits ahead / 0 behind master, ~6586 lines master lacks: `docs/local-view/index.html` weave-progress page, 5083-line medallion SVG, tests, big `tools/wave-plan-server.py` change). The 38 merged sibling branches (bikar 22, qiyas 9, sacred-patterns 7) were DELETED from their origins 2026-08-30 (user: "Delete all 38"). **The two-wip-branch decision was CLOSED 2026-08-31:** `wip/weave-progress-page` **merged** into sacred-patterns master (PR #43 → `0d3ad1e`, squash — weave-progress page + medallion SVG + Playwright pixel-diff specs + this-repo auto-memory checked in) and its branch deleted; `wip/react-d3-2024` **kept** as cited prior art, its "what do we do with this?" question folded into a new **d3-integration scoping doc** (3d-models `docs/d3-integration-design.md`, PR #111 → `65f3fb3`) — the doc audits the branch (dep-upgrade half dead, master did d3 5→7/TS/webpack independently; live assets = the React-on-d3 `useEffect` bridge + artwork-registry dropdown + geometry/draw vocabulary), and establishes qiyas integrates with d3 by DATA CONTRACT (Python, never runs d3) while bikar is not a d3 project and sacred-patterns is, so the real open calls are Q-HOME (where a shared d3 layer lives) / Q-SHELL (React or not) / Q-VOCAB (share the vocabulary or not) / Q-DATA (qiyas overlay shape) — all handed to the user, nothing scheduled; the rosette→pin explorer is named as the first reference consumer, and the doc is scoping/not-ground-audited. **Final remote state: bikar=`main` only; qiyas=`main` only; 3d-models=`master`+`gh-pages`; sacred-patterns=`master`+`gh-pages`+`wip/react-d3-2024` (one kept branch, the cited prior art).** **d3-integration decisions SETTLED + qiyas API scoped 2026-08-31:** the four d3 calls were answered by the user and recorded in `docs/d3-integration-design.md` §5 (3d-models #113 → `75cf71d`) — **Q-HOME** = opt-in converter on a d3-agnostic bikar core (core emits generic, generally-consumable constructs; separable optional layer maps to d3; nobody forced to use d3), **Q-SHELL** = plain/Lit shell not React (react-d3-2024's React shell NOT adopted, only its vocabulary + bridge idea), **Q-VOCAB** = converge on a common naming convention, refactoring either sacred-patterns or bikar to align is authorized, **Q-DATA** = qiyas emits per-shape `{id,x,y,status}` (but likely a documented JOIN of `/encoding`+`/diff`, not new fields — encoding already carries shape geometry). Companion qiyas scoping doc `qiyas/docs/design/data-model-http-api.md` (qiyas #23 → `0c8f813`): qiyas ALREADY has a versioned pydantic contract (`schema.py` SCHEMA_VERSION 1.27 + generated JSON Schema in `contract/schemas/`, drift-gated) AND a FastAPI server (`qiyas serve` port 8731, `/encoding` `/diff` `POST /deconstruct`, Swagger at `/docs` off in `--hosted`); the only gap for a clean Swagger data API is the handlers return raw `JSONResponse` with no `response_model=` so the schema never reaches `/openapi.json` — **do NOT add Flask (second framework = forbidden divergence), extend the existing FastAPI app.** Open follow-ons tracked as tasks #29 (bikar-studio public-surface keystone — gates d3 Phase 1 AND qiyas D-API-2, same decision from two docs), #30 (add `response_model=` to qiyas data endpoints), #31 (d3 Phase 1 rosette explorer reference surface), #32 (d3 Phase 2 qiyas overlay — measure the join first). **Task #29 CLOSED 2026-08-31** — bikar-studio public-surface keystone RESOLVED (see [[bikar-studio-access]], 3d-models #115 → `3b80985`): only public entry is `bikar.naqshcoffee.com`, org-GitHub-gated — internal audience, internet-reachable. **Task #30 SHIPPED 2026-08-31** (qiyas #24 → `62124f4`): added `response_model=` to `/encoding` (Encoding), `/diff` (Diff), `POST /deconstruct` (Encoding) in `review/server.py` — a returned Response passes through untouched so bytes are byte-identical, response_model only feeds OpenAPI; `/openapi.json` now $refs Encoding+Diff and pulls every nested shape into `components/schemas`. Parametrized regression test fails without the decorators. Local CI parity all green (unit suite 2395 passed, mypy --strict, ruff, format-check, import-linter L1-L6); GitHub Actions showed red but it was the billing block (steps=0, 2-3s — nothing measured); qiyas local `main` left diverged/behind origin (squash history), origin authoritative. **Still open: #31 (unblocked by #29), #32 (measure the Q-DATA join first).** pending user: contract+schema publish, coffee-house-sites#1, print prototype (on hold, Bambu A1/P1S/X1C class)
- [bikar studio access](bikar-studio-access.md) — only public entry is bikar.naqshcoffee.com, gated behind org GitHub sign-in; internal audience, internet-reachable (settles the public-surface keystone)
- [gh auto-merge footgun](gh-auto-merge-footgun.md) — `--auto` merges immediately on NaqshCoffee repos; poll to green instead
- [3d-models use-case hook](3d-models-use-case-hook.md) — pre-commit dispatcher blocks pointer-file commits without map update; USE_CASES_OK=1 override
- [PR flow for all repos](pr-flow-for-all-repos.md) — every change (docs included) goes branch→PR→merge, never direct-to-master; marketplace repo is the exception
- [Stacked-PR stranding](stacked-pr-stranding.md) — never base a PR on another open PR's branch here; merge lands in the parent, not master, and strands it if the parent goes stale

Moved out of this repo's memory when auto-memory became per-repo (2026-08-16): `amazon-scripts-repo` → amazon-scripts, `marketplace-repo-mirror-and-held-branch` → oeid-claude-plugin-marketplace, `check-for-existing-e2e-before-reporting-blocked` and `supabase-paused-project-timeout` → bikar. Named without links because the files are no longer here and a dead relative link is a D1 gate failure.
