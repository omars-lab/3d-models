# Fixture: D1 must fire on a relative link that does not resolve

Asserted FAIL fixture for rule D1 (K9). This reproduces the defect that shipped
in `hemisphere-split-design.md`, whose status line linked an audit file the repo
did not contain.

Revised after an adversarial grounding audit
([research/hemisphere-split-grounding-audit.md](./hemisphere-split-grounding-audit.md)).

Expected: exactly one finding, `D1 (K9)`.
